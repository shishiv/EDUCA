# EDUCA - auditoria corretiva bounded

Data: 2026-08-10
Branch: `fm/educa-audit-correct-session-20260810`
Escopo: trabalho EDUCA tocado nesta sessão, após o PR #86.

## Veredito

O código desta branch corrige B1-B9, B11 e B12 dentro do contrato aprovado.

O sandbox público continua antigo e não recebeu esta branch. Portanto, não está
aprovado como demo determinística. O workflow de reset e o CI continuam ausentes.

Não houve deploy, merge, reset do banco público ou mutação no repositório educa-site.

## Reconciliação

- EDUCA PR #86: merged em `2026-08-10T00:02:03Z`.
- educa-site PR #14: merged em `2026-08-09T23:50:51Z`.
- educa-site PR #15: merged em `2026-08-10T00:08:15Z`.
- `origin/main` do EDUCA foi conferido em `3ba139c`.
- Esta branch contém os commits corretivos locais e não foi mesclada.
- Os clones locais usados nesta auditoria pertencem a worktrees isolados.
- `.github/workflows/` não existe localmente nem no `main` remoto.
- O `DEMO.md` e o `CONTEXT.md` agora registram que reset semanal e CI estão ausentes.
- As decisões `sandbox-ar` e `automacao-sandbox` continuam pendentes.

## Runtime público reexecutado

URL: `https://educa-demo.vercel.app`

Receipts observados com chrome-devtools-axi:

- `/api/health`: `200`, `{"status":"healthy"}`.
- Dashboard: `51` alunos, `9` turmas e `50` professores ativos.
- A mesma tela pública exibiu contagens de turma diferentes do contrato canônico.
- `/relatorios/bolsa-familia`: `0` alunos e `Bolsa Família: Sem Alertas`.
- `/api/compliance/warnings`: `warnings: []`.
- `/api/dashboard/alerts`: ação de chamada com `href: "/chamada"`.
- `/dashboard/turmas/...101`: `87.5%` fixo e `0` sessões.
- `/dashboard/diario`: registros antigos de julho, compatíveis com a fonte legada.
- Banner, Diário, Relatórios e Configurações não aparecem na navegação pública.

Conclusão: o público ainda reproduz o deploy e o banco descritos no S0. A branch
não pode ser chamada de runtime público corrigido sem deploy e reset controlados.

## Prova local real

Ambiente: Supabase local com seed demo, app servido por portless em
`https://educa-audit-session.localhost`. Nenhuma porta numerada foi usada como
evidência.

Comando de validação do banco:

```text
SUPABASE_DEMO_DB_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres pnpm demo:validate

[PASS] count_escolas: 3 == 3
[PASS] count_users: 11 == 11
[PASS] count_turmas: 5 == 5
[PASS] count_alunos: 50 == 50
[PASS] count_matriculas: 50 == 50
[PASS] count_sessoes_aula: 100 == 100
[PASS] count_frequencia: 1000 == 1000
[PASS] rel_frequencia_vinculos: 0
[PASS] rel_frequencia_sessao_da_turma: 0
[PASS] alerta_bolsa_familia: 3 caso(s) abaixo de 80%
[PASS] alerta_caso_designado: caso de alerta designado presente
[PASS] determinismo_presenca_por_aluno: 0 divergências
VALIDACAO OK - dataset determinístico e synthetic-only conforme o contrato.
```

Provas browser locais:

- Login demo real: redirecionou para `/dashboard`.
- Dashboard desktop: `50` alunos, `5` turmas, `90.2%` de frequência e `10`
  professores ativos. Cada turma exibiu `10 alunos`.
- Dashboard mobile: banner demo, Diário e Relatórios aparecem na navegação.
- Diário desktop e mobile: a lista exibiu sessões canônicas de agosto, com
  frequências como `9/1` e `7/3`. O detalhe exibiu `10` alunos, `10` presentes,
  `0` ausentes e frequência `100%`.
- Relatório Bolsa Família desktop e mobile: `26` alunos, `4` críticos no mês
  atual e Miguel Alves dos Santos visível com `40%`. No período completo do seed,
  o validador comprova os três casos abaixo de 80%.
- `/api/compliance/warnings`: retornou um warning real com `count: 4` no mês
  atual e ação `/relatorios/bolsa-familia`.
- Página da turma `...101`: `89.5%` calculado, `10` sessões e `10` concluídas,
  com contagens reais de presentes e ausentes.
- Nova Aula para o admin: o diálogo mostra `Perfil com acesso de visualização`
  e mantém `Salvar Aula` desabilitado. Nenhum insert foi tentado.
- Dashboard do professor: uma conta sintética temporária mostrou `1` turma,
  `10` alunos ativos e `Chamada pendente`. A conta foi removida e a atribuição
  original foi restaurada; `demo:validate` voltou a passar com 11 usuários.

Capturas após a correção ficam em `/tmp/educa-audit-after-*.png` nesta sessão.
As capturas S0 anteriores permanecem no diretório do scout fornecido no brief.

## Estado dos bugs

| Bug | Branch/local após a correção | Público atual | Próximo ship |
| --- | --- | --- | --- |
| B1 | Corrigido: contagem de matrículas ativas, sem `Math.random()` | Reproduz | Deploy do PR e prova pública |
| B2 | Corrigido: `situacao = 'ativa'` | Reproduz | Deploy do PR e prova dos três casos |
| B3 | Corrigido: conta professores ativos | Reproduz | Deploy do PR e prova dos cards |
| B4 | Corrigido: leitura literal pública no cliente | Reproduz | Build/deploy e prova desktop/mobile |
| B5 | Corrigido: `sessoes_aula` e `frequencia.sessao_id` | Runtime antigo lê legado | Deploy aprovado e reset controlado |
| B6 | Corrigido: warning real Bolsa Família | Reproduz com `warnings: []` | Deploy e prova do endpoint |
| B7 | Corrigido: `/dashboard/turmas` | Reproduz com `/chamada` | Deploy e clique real |
| B8 | Corrigido: média e sessões canônicas calculadas | Reproduz com `87.5%` e zero sessões | Deploy e prova da turma |
| B9 | Corrigido: feedback inline view-only para admin | Não revalidado por escrita pública | Deploy e prova segura sem POST público |
| B10 | Aberto: workflows ausentes por decisão pendente | Aberto | Ship separado `automacao-sandbox` |
| B11 | Corrigido: dashboard real do professor | Sem prova pública desta branch | Deploy e login sintético controlado |
| B12 | Corrigido: teste usa data de São Paulo | N/A | Nenhum ship adicional |

## Ships concretos ainda necessários

### P0 - Publicar o código corrigido

- Escopo: deploy da branch aprovada no projeto Vercel correto.
- Arquivos: todos os commits corretivos desta branch.
- Teste: `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build` com
  `NEXT_PUBLIC_PILOT_MODE=true`.
- Prova: URL de deployment, commit, status Ready, headers públicos e capturas
  desktop/mobile dos fluxos críticos.
- Não executado nesta tarefa: deploy externo.

### P1 - Reconvergir o sandbox público

- Escopo: somente após a decisão `sandbox-ar` e a aprovação da operação
  `TRUNCATE ... CASCADE` no banco público.
- Arquivos: workflow aprovado e configuração externa, em ship separado.
- Teste: `pnpm seed:demo` e `pnpm demo:validate` com os três secrets demo.
- Prova: marker, âncora, contagens, relacionamentos, fingerprints e Auth.
- Não executar localmente contra o projeto público.

### P1 - Restaurar automação

- Escopo: `ci.yml` com typecheck, lint, testes e E2E; `demo-reset.yml` com
  seed e validação agendados.
- Arquivos: `.github/workflows/ci.yml` e `.github/workflows/demo-reset.yml`.
- Teste: runs verdes no GitHub e primeiro reset controlado com receipt antes/depois.
- Prova: secrets presentes sem impressão, logs sem PII e dataset convergente.
- Hold: `automacao-sandbox`. Este audit não decidiu nem restaurou os workflows.

## Testes executados

```text
cd app && pnpm typecheck
OK

cd app && pnpm lint
ESLint: 0 errors, 623 warnings existentes

cd app && pnpm test
Test Files 49 passed | 1 skipped
Tests 736 passed | 17 skipped

NEXT_PUBLIC_PILOT_MODE=true pnpm build
Build concluído com sucesso; avisos existentes de middleware e Edge Runtime.
```

O teste de workflow agora fixa `getTodaySaoPaulo()` e não depende de UTC local.
Os testes de demo também cobrem a leitura dinâmica do flag no ambiente de teste.
`pnpm demo:verify-sql` também passou com fingerprints idênticos após reset
repetido na âncora `2026-07-01`.

## Decisões para o captain

Nenhuma decisão nova foi aberta nesta auditoria.

Mantêm-se os holds já registrados:

- `sandbox-ar`: autorizar ou manter congelado o runtime público.
- `automacao-sandbox`: restaurar ou não os workflows de CI e reset.

O contrato desta tarefa resolveu as escolhas locais necessárias para o código:
fonte canônica `sessoes_aula`, limiar de conformidade Bolsa Família em 80% e
admin view-only no Diário. Isso não autoriza reset ou deploy público.
