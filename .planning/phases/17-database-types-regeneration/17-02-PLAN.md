---
phase: 18-database-types-regeneration
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - gestao_fronteira/types/database.ts
autonomous: true

must_haves:
  truths:
    - "TypeScript types match production database schema"
    - "relatorios_descritivos type available in Database type"
    - "Build passes with regenerated types"
  artifacts:
    - path: "gestao_fronteira/types/database.ts"
      provides: "Generated Supabase TypeScript types"
      contains: "relatorios_descritivos"
      min_lines: 1600
  key_links:
    - from: "gestao_fronteira/types/database.ts"
      to: "relatorios_descritivos table"
      via: "type generation"
      pattern: "relatorios_descritivos.*Row.*matricula_id"
---

<objective>
Regenerate TypeScript types from Supabase production schema.

Purpose: Current types/database.ts is outdated - missing relatorios_descritivos (just created), calendario_escolar, feature_flags, escola_feature_flags, and several columns (nis, bolsa_familia on alunos, primeiro_login/senha_padrao/data_ultimo_acesso on users).

Output: Updated types/database.ts with all production tables and columns.
</objective>

<execution_context>
@C:\Users\shiv\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\shiv\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-database-types-regeneration/18-RESEARCH.md
@.planning/phases/18-database-types-regeneration/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate TypeScript types from production</name>
  <files>gestao_fronteira/types/database.ts</files>
  <action>
Use mcp__supabase__generate_typescript_types with project_id "SUPABASE-PROJECT-REF" to generate fresh types.

Write the generated output to gestao_fronteira/types/database.ts, completely replacing the existing file.

The generated file should include:
1. All tables from production including:
   - relatorios_descritivos (newly created)
   - calendario_escolar
   - feature_flags
   - escola_feature_flags
2. All columns including:
   - alunos.nis, alunos.bolsa_familia
   - users.primeiro_login, users.senha_padrao, users.data_ultimo_acesso
3. All views including:
   - vw_alunos_risco_bolsa_familia
   - vw_frequencia_completa
   - audit_summary
4. All functions
5. Type helpers (Tables, TablesInsert, TablesUpdate, Enums, CompositeTypes)
  </action>
  <verify>Read the generated file and confirm it contains "relatorios_descritivos" and all expected tables</verify>
  <done>types/database.ts contains all production tables, views, and columns</done>
</task>

<task type="auto">
  <name>Task 2: Verify TypeScript compilation</name>
  <files>none (verification only)</files>
  <action>
Run TypeScript type check to ensure the new types work correctly:

```bash
cd gestao_fronteira && pnpm.cmd typecheck
```

If there are type errors, they likely indicate code that was using incorrect types. Common issues:
1. Code using `wizard_completed` on users (column was removed from production)
2. Code expecting different column types

Fix any breaking changes by:
- Updating code to match new types
- OR if column truly doesn't exist, remove the usage
  </action>
  <verify>pnpm typecheck exits with code 0</verify>
  <done>TypeScript compilation passes with regenerated types</done>
</task>

<task type="auto">
  <name>Task 3: Verify build passes</name>
  <files>none (verification only)</files>
  <action>
Run Next.js build to ensure everything compiles:

```bash
cd gestao_fronteira && pnpm.cmd build
```

This is the ultimate verification that:
1. Types are syntactically correct
2. All imports resolve
3. No runtime issues from type mismatches
  </action>
  <verify>pnpm build completes successfully</verify>
  <done>Production build passes with new types</done>
</task>

</tasks>

<verification>
1. types/database.ts updated: File contains relatorios_descritivos type
2. Missing tables added: calendario_escolar, feature_flags, escola_feature_flags present
3. Missing columns added: alunos.nis, alunos.bolsa_familia present
4. TypeScript passes: pnpm typecheck succeeds
5. Build passes: pnpm build succeeds
</verification>

<success_criteria>
- types/database.ts regenerated from production schema
- All 23+ tables represented in types
- relatorios_descritivos type has all BNCC campos columns
- TypeScript compilation succeeds
- Next.js build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/18-database-types-regeneration/18-02-SUMMARY.md`
</output>
