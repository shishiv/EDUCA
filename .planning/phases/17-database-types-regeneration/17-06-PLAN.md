---
phase: 18-database-types-regeneration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/lib/api/base.ts
  - gestao_fronteira/lib/api/enhanced-base.ts
  - gestao_fronteira/lib/api/attendance.ts
  - gestao_fronteira/lib/api/enhanced-attendance.ts
  - gestao_fronteira/lib/api/dashboard-stats.ts
  - gestao_fronteira/lib/api/feature-flags.ts
  - gestao_fronteira/lib/api/students.ts
  - gestao_fronteira/lib/api/schools.ts
  - gestao_fronteira/lib/api/classes.ts
  - gestao_fronteira/lib/api/users.ts
  - gestao_fronteira/lib/api/reports.ts
  - gestao_fronteira/lib/api/grades.ts
  - gestao_fronteira/lib/api/configs.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "lib/api/* files compile without type errors"
    - "Supabase queries use correct column names"
    - "Error handling uses proper types"
  artifacts:
    - path: "gestao_fronteira/lib/api/base.ts"
      provides: "Type-safe API base"
      min_lines: 50
  key_links:
    - from: "lib/api/*"
      to: "types/database.ts"
      via: "type imports"
      pattern: "Database"
---

<objective>
Fix type errors in lib/api layer.

Purpose: The lib/api layer contains 50+ type errors related to:
- Supabase query column mismatches
- Error type handling
- null vs undefined mismatches
- Missing type annotations

Output: All lib/api files compile without type errors.
</objective>

<execution_context>
@C:\Users\shiv\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\shiv\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-database-types-regeneration/18-VERIFICATION.md
@gestao_fronteira/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix base API types</name>
  <files>
gestao_fronteira/lib/api/base.ts
gestao_fronteira/lib/api/enhanced-base.ts
  </files>
  <action>
Fix fundamental type issues in base API files:

1. Update error handling types:
   ```typescript
   // Instead of: throw new Error({ message, error })
   // Use: throw new Error(message)
   // Or create proper ApiError class

   class ApiError extends Error {
     constructor(
       message: string,
       public code?: string,
       public details?: unknown
     ) {
       super(message);
       this.name = 'ApiError';
     }
   }
   ```

2. Fix null vs undefined in return types:
   ```typescript
   // If database returns null, handle consistently
   return data ?? undefined; // converts null to undefined
   ```

3. Update generic type parameters to match Database types:
   ```typescript
   import type { Database } from '@/types/database';
   type Tables = Database['public']['Tables'];
   ```

4. Fix any property access issues by adding proper type guards
  </action>
  <verify>pnpm.cmd typecheck 2>&1 | grep "lib/api/base" shows 0 errors</verify>
  <done>Base API files have proper type definitions</done>
</task>

<task type="auto">
  <name>Task 2: Fix domain-specific API files</name>
  <files>
gestao_fronteira/lib/api/attendance.ts
gestao_fronteira/lib/api/enhanced-attendance.ts
gestao_fronteira/lib/api/dashboard-stats.ts
gestao_fronteira/lib/api/students.ts
gestao_fronteira/lib/api/schools.ts
gestao_fronteira/lib/api/classes.ts
  </files>
  <action>
Fix type errors in each file:

1. attendance.ts / enhanced-attendance.ts:
   - Use matricula_id not aluno_id for frequencia queries
   - Use sessao_id not sessao_aula_id
   - Fix return type nullability

2. dashboard-stats.ts:
   - Ensure statistics queries use correct column names
   - Fix any aggregate type issues

3. students.ts:
   - alunos table has specific columns (check database.ts)
   - responsavel_id is correct
   - nis and bolsa_familia exist

4. schools.ts:
   - Fix boolean | null issues (use nullish coalescing)
   - Ensure escola queries use correct columns

5. classes.ts:
   - turmas table structure
   - professor_id relationships
  </action>
  <verify>pnpm.cmd typecheck 2>&1 | grep -E "lib/api/(attendance|dashboard|students|schools|classes)" shows fewer errors</verify>
  <done>Domain API files compile</done>
</task>

<task type="auto">
  <name>Task 3: Fix remaining lib/api files</name>
  <files>
gestao_fronteira/lib/api/feature-flags.ts
gestao_fronteira/lib/api/users.ts
gestao_fronteira/lib/api/reports.ts
gestao_fronteira/lib/api/grades.ts
gestao_fronteira/lib/api/configs.ts
  </files>
  <action>
Fix remaining lib/api files:

1. feature-flags.ts:
   - Check if feature_flags table exists in database.ts
   - If exists, use correct column names
   - If not, stub the functions

2. users.ts:
   - users table columns from database.ts
   - No nome_completo - check actual column names
   - primeiro_login, senha_padrao, data_ultimo_acesso exist

3. reports.ts:
   - relatorios_descritivos now exists
   - Use correct column names from database.ts

4. grades.ts:
   - notas table structure
   - matricula_id relationships

5. configs.ts:
   - Check table existence
   - Fix query types
  </action>
  <verify>pnpm.cmd typecheck 2>&1 | grep "lib/api" | wc -l shows significantly fewer errors</verify>
  <done>All lib/api files compile</done>
</task>

</tasks>

<verification>
1. Base types: Error classes properly defined
2. Query types: All queries use correct column names
3. Null handling: Consistent null/undefined conversion
4. Compilation: pnpm typecheck passes for lib/api
</verification>

<success_criteria>
- All lib/api/*.ts files compile
- No column name mismatches in Supabase queries
- Error types are properly structured
- null vs undefined handled consistently
</success_criteria>

<output>
After completion, create `.planning/phases/18-database-types-regeneration/18-06-SUMMARY.md`
</output>
