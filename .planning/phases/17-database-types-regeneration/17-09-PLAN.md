---
phase: 18-database-types-regeneration
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/components/layout/header.tsx
  - gestao_fronteira/components/layout/sidebar.tsx
  - gestao_fronteira/components/layout/mobile-header.tsx
  - gestao_fronteira/components/layout/mobile-sidebar.tsx
  - gestao_fronteira/components/ui/calendar.tsx
  - gestao_fronteira/components/ui/modal-renderer.tsx
  - gestao_fronteira/components/ui/responsive-data-table.tsx
  - gestao_fronteira/contexts/escola-context.tsx
  - gestao_fronteira/contexts/search-context.tsx
  - gestao_fronteira/contexts/session-realtime-context.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Layout components compile without errors"
    - "UI components use correct library types"
    - "Contexts provide type-safe values"
  artifacts:
    - path: "gestao_fronteira/components/layout/header.tsx"
      provides: "Type-safe header component"
      min_lines: 50
  key_links:
    - from: "components/layout/*"
      to: "contexts/*"
      via: "context hooks"
      pattern: "useEscola|useSearch|useSession"
---

<objective>
Fix type errors in layout, UI components, and contexts.

Purpose: These foundational components have 30+ errors related to:
- Layout component type issues
- react-day-picker calendar component types
- Context value type mismatches
- DataTable generic types

Output: All layout, UI, and context files compile.
</objective>

<execution_context>
@C:\Users\shiv\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\shiv\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-database-types-regeneration/18-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix layout components</name>
  <files>
gestao_fronteira/components/layout/header.tsx
gestao_fronteira/components/layout/sidebar.tsx
gestao_fronteira/components/layout/mobile-header.tsx
gestao_fronteira/components/layout/mobile-sidebar.tsx
gestao_fronteira/components/layout/index.ts
  </files>
  <action>
Fix type errors in layout components:

1. Check what user properties are available:
   - Import user type from database.ts
   - Ensure property access matches schema

2. Fix any context value type issues:
   - EscolaContext value shape
   - SessionContext value shape

3. Fix null safety:
   - Add optional chaining for potentially null values
   - Handle loading states

4. Fix index.ts exports if needed

5. Common patterns to fix:
   ```typescript
   // Instead of: user.name
   // Check actual column: user.nome_usuario or user.email

   // Instead of: escola.name
   // Use: escola.nome
   ```
  </action>
  <verify>pnpm.cmd typecheck 2>&1 | grep "components/layout" shows 0 errors</verify>
  <done>Layout components compile</done>
</task>

<task type="auto">
  <name>Task 2: Fix UI components</name>
  <files>
gestao_fronteira/components/ui/calendar.tsx
gestao_fronteira/components/ui/modal-renderer.tsx
gestao_fronteira/components/ui/responsive-data-table.tsx
  </files>
  <action>
Fix UI component type errors:

1. calendar.tsx:
   - Error: 'IconLeft' does not exist on CustomComponents
   - react-day-picker v9 changed API:
   ```typescript
   // Old (v8):
   components={{ IconLeft: ChevronLeft }}

   // New (v9):
   components={{ Chevron: ({ orientation }) =>
     orientation === 'left' ? <ChevronLeft /> : <ChevronRight />
   }}
   ```
   - Or check installed version and use correct API

2. modal-renderer.tsx:
   - Fix any generic type issues
   - Ensure proper children typing

3. responsive-data-table.tsx:
   - Fix generic type constraints
   - Ensure column definitions are properly typed
   - Handle null values in data

4. Check package versions for breaking changes:
   ```bash
   grep "react-day-picker" package.json
   ```
  </action>
  <verify>pnpm.cmd typecheck 2>&1 | grep "components/ui" shows 0 errors</verify>
  <done>UI components compile</done>
</task>

<task type="auto">
  <name>Task 3: Fix contexts</name>
  <files>
gestao_fronteira/contexts/escola-context.tsx
gestao_fronteira/contexts/search-context.tsx
gestao_fronteira/contexts/session-realtime-context.tsx
  </files>
  <action>
Fix context type errors:

1. escola-context.tsx:
   - Ensure context value matches provider value
   - Type escola data from database.ts
   - Handle loading/error states

2. search-context.tsx:
   - Fix any search result type issues
   - Handle async search properly

3. session-realtime-context.tsx:
   - Update session types to match database schema
   - inicio_aula not hora_inicio
   - Handle realtime subscription types

Pattern for context fixes:
```typescript
interface EscolaContextValue {
  escola: Tables<'escolas'>['Row'] | null;
  loading: boolean;
  error: Error | null;
}

const EscolaContext = createContext<EscolaContextValue | undefined>(undefined);
```
  </action>
  <verify>pnpm.cmd typecheck 2>&1 | grep "contexts/" shows 0 errors</verify>
  <done>Contexts provide type-safe values</done>
</task>

</tasks>

<verification>
1. Layout: All layout components render without type errors
2. UI: Calendar and other UI components use correct library types
3. Contexts: All context values properly typed
4. Compilation: pnpm typecheck passes for these files
</verification>

<success_criteria>
- components/layout/*.tsx compile
- components/ui/*.tsx compile
- contexts/*.tsx compile
- No library API mismatches
</success_criteria>

<output>
After completion, create `.planning/phases/18-database-types-regeneration/18-09-SUMMARY.md`
</output>
