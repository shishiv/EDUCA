---
phase: 15-technical-debt-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/page.tsx
  - gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/relatorio/page.tsx
  - gestao_fronteira/app/(dashboard)/dashboard/diario/page.tsx
autonomous: true

must_haves:
  truths:
    - "Diario page edit modal opens with existing vivencia data"
    - "Diario page delete confirms and removes vivencia"
    - "Relatorio page saves draft to API"
    - "Relatorio page finalizes report via API"
  artifacts:
    - path: "gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/page.tsx"
      provides: "Student diary page with working edit/delete"
    - path: "gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/relatorio/page.tsx"
      provides: "Report page with working save/finalize"
    - path: "gestao_fronteira/app/(dashboard)/dashboard/diario/page.tsx"
      provides: "Class diary with working edit modal"
  key_links:
    - from: "diario/page.tsx"
      to: "lib/api/vivencias.ts"
      via: "vivenciasApi.update"
      pattern: "vivenciasApi\\.(update|delete)"
---

<objective>
Complete TODOs in pages for diario edit/delete and relatorio save/finalize

Purpose: Implement remaining CRUD operations per CLN-01 (TODO cleanup in pages)
Output: Full edit/delete/save functionality working in diary and report pages
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-technical-debt-cleanup/15-RESEARCH.md

@gestao_fronteira/lib/api/vivencias.ts
@gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/page.tsx
@gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/relatorio/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement edit/delete in student diary page</name>
  <files>gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/page.tsx</files>
  <action>
Implement the TODOs at lines ~119 and ~124:

1. Read the current page to understand existing patterns
2. For EDIT flow (line ~119):
   - Find the edit button handler (handleEdit or similar)
   - Open existing NewVivenciaModal or similar in edit mode
   - Pass the vivencia data to populate form
   - On submit, call vivenciasApi.update(vivenciaId, updatedData)
   - Use useMutation with onSuccess -> invalidateQueries(['vivencias'])
   - Show toast on success: "Vivencia atualizada com sucesso"
   - Show toast on error with error message

3. For DELETE flow (line ~124):
   - Add confirmation dialog before delete (use AlertDialog from shadcn/ui)
   - Message: "Tem certeza que deseja excluir esta vivencia?"
   - On confirm, call vivenciasApi.delete(vivenciaId)
   - Use useMutation with onSuccess -> invalidateQueries(['vivencias'])
   - Show toast on success: "Vivencia excluida com sucesso"
   - Show toast on error with error message

4. Remove the TODO comments after implementation

Use patterns from existing CRUD pages (e.g., alunos delete in students list).
  </action>
  <verify>
pnpm typecheck - no errors
grep "TODO.*edit" alunos/[id]/diario/page.tsx - returns 0 matches
grep "TODO.*delete" alunos/[id]/diario/page.tsx - returns 0 matches
grep "vivenciasApi.update" alunos/[id]/diario/page.tsx - exists
grep "vivenciasApi.delete" alunos/[id]/diario/page.tsx - exists
  </verify>
  <done>Student diary page has working edit and delete with confirmation dialogs</done>
</task>

<task type="auto">
  <name>Task 2: Implement save/finalize in relatorio page</name>
  <files>gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/relatorio/page.tsx</files>
  <action>
Implement the TODOs at lines ~164 and ~175:

1. Read the current page to understand report structure
2. For SAVE DRAFT flow (line ~164):
   - Find the existing save handler
   - Create API call to save report draft:
     - If using vivencias endpoint, call vivenciasApi.updateReport or similar
     - If separate reports table, may need new API method
   - Save current form state as draft (status: 'rascunho')
   - Show toast: "Rascunho salvo com sucesso"

3. For FINALIZE flow (line ~175):
   - Add confirmation before finalizing (reports typically can't be un-finalized)
   - Message: "Ao finalizar, o relatorio nao podera ser editado. Continuar?"
   - On confirm:
     - Update report status to 'finalizado'
     - Call vivenciasApi.finalizeReport(reportId) or similar
     - Disable all form fields after finalization
   - Show toast: "Relatorio finalizado com sucesso"

4. Handle the case where report is already finalized (show read-only mode)
5. Remove the TODO comments after implementation

Check if VivenciasApiService has report methods, or if need to add them.
  </action>
  <verify>
pnpm typecheck - no errors
grep "TODO.*save" relatorio/page.tsx - returns 0 matches
grep "TODO.*finalize" relatorio/page.tsx - returns 0 matches
  </verify>
  <done>Report page saves drafts and finalizes reports via API with confirmation</done>
</task>

<task type="auto">
  <name>Task 3: Implement edit modal in class diary page</name>
  <files>gestao_fronteira/app/(dashboard)/dashboard/diario/page.tsx</files>
  <action>
Implement the TODO at line ~395 for opening edit modal:

1. Read the current page to find the edit handler
2. Find or create state for edit modal:
   - editingLesson: SessaoAula | null
   - isEditModalOpen: boolean
3. Implement handleEditLesson(lesson):
   - Set editingLesson to the lesson data
   - Open the modal (setIsEditModalOpen(true))
4. Pass lesson data to existing NewLessonModal or create edit variant:
   - Modal should pre-populate with lesson data
   - On submit, call appropriate API to update
   - If using sessoes_aula table, may need EnhancedAttendanceService method
5. Also migrate the direct Supabase delete query (line ~402-412) to API service:
   - Move to EnhancedAttendanceService or ClassDiaryApiService
   - Add deleteSession(sessionId) method if not exists
6. Remove the TODO comment after implementation
  </action>
  <verify>
pnpm typecheck - no errors
grep "TODO.*edit" diario/page.tsx - returns 0 matches
grep "supabase.from.*sessoes_aula.*delete" diario/page.tsx - returns 0 matches (migrated to API)
  </verify>
  <done>Class diary page edit modal opens with lesson data, delete uses API service</done>
</task>

</tasks>

<verification>
1. pnpm typecheck passes
2. pnpm lint passes
3. grep -r "TODO" in modified files - no remaining TODOs for these features
4. Edit/delete/save/finalize flows work in browser
</verification>

<success_criteria>
- Student diary: Edit opens modal with data, delete confirms and removes
- Relatorio: Save persists draft, Finalize locks report
- Class diary: Edit modal opens with lesson data
- All direct Supabase queries migrated to API services
- No TODO comments remain for these features
- Toast feedback on all actions (success/error)
</success_criteria>

<output>
After completion, create `.planning/phases/15-technical-debt-cleanup/15-04-SUMMARY.md`
</output>
