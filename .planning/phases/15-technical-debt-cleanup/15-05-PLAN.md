---
phase: 15-technical-debt-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/components/attendance/AbrirAulaWorkflow.tsx
  - gestao_fronteira/components/role-specific-dashboards.tsx
  - gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/boletim/page.tsx
autonomous: true

must_haves:
  truths:
    - "AbrirAulaWorkflow creates class session via API"
    - "Role dashboards show real baixa frequencia count from data"
    - "Role dashboards show real grade from notas table"
    - "Boletim page exports PDF with student grades"
  artifacts:
    - path: "gestao_fronteira/components/attendance/AbrirAulaWorkflow.tsx"
      provides: "Workflow that actually opens class session"
    - path: "gestao_fronteira/components/role-specific-dashboards.tsx"
      provides: "Dashboards with calculated stats"
  key_links:
    - from: "AbrirAulaWorkflow.tsx"
      to: "lib/api/enhanced-attendance.ts"
      via: "createSession or similar"
      pattern: "enhancedAttendanceApi\\."
---

<objective>
Complete TODOs in components for dashboard calculations and AbrirAulaWorkflow

Purpose: Implement component TODOs per CLN-02 (dashboard calculations, workflow logic)
Output: Working class session creation, real dashboard calculations, PDF export
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-technical-debt-cleanup/15-RESEARCH.md

@gestao_fronteira/components/attendance/AbrirAulaWorkflow.tsx
@gestao_fronteira/components/role-specific-dashboards.tsx
@gestao_fronteira/lib/api/enhanced-attendance.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AbrirAulaWorkflow session creation</name>
  <files>gestao_fronteira/components/attendance/AbrirAulaWorkflow.tsx</files>
  <action>
Implement the TODO at line ~23 for opening class session:

1. Read current component to understand workflow steps
2. Find the submit/confirm handler that currently has TODO
3. Implement actual session creation logic:
   - Gather selected turma, disciplina, date from workflow state
   - Call enhancedAttendanceApi.createSession() or similar method
   - If method doesn't exist, add it to EnhancedAttendanceService:
     ```typescript
     async createSession(input: CreateSessionInput): Promise<SessaoAula>
     ```
4. On success:
   - Navigate to the newly created session (frequencia page)
   - Or show success toast and close workflow
5. On error:
   - Show error toast with message
   - Keep workflow open for retry
6. Add loading state during API call
7. Remove the TODO comment

Check EnhancedAttendanceService for existing session creation methods first.
  </action>
  <verify>
pnpm typecheck - no errors
grep "TODO.*logic.*open" AbrirAulaWorkflow.tsx - returns 0 matches
grep "createSession\|enhancedAttendanceApi" AbrirAulaWorkflow.tsx - exists
  </verify>
  <done>AbrirAulaWorkflow creates session via API and navigates to frequencia page</done>
</task>

<task type="auto">
  <name>Task 2: Implement real calculations in role-specific dashboards</name>
  <files>gestao_fronteira/components/role-specific-dashboards.tsx</files>
  <action>
Replace hardcoded values with real calculations at lines ~105, ~396, ~587, ~790:

1. Read component to understand dashboard structure for each role
2. For BAIXA FREQUENCIA count (lines ~105, ~396, ~587):
   - Currently shows hardcoded number
   - Need to count students with frequency < 80%
   - Use attendanceApi or similar to get frequency data
   - Filter students where frequencia < 0.80
   - Replace hardcoded with: `baixaFrequenciaCount` state variable
   - Add useQuery to fetch this data, or compute from existing data

3. For GRADE value (line ~790):
   - Currently shows hardcoded grade
   - Fetch actual grade from notas table via gradesApi
   - Calculate average if multiple grades exist
   - Replace hardcoded with: `mediaGeral` state variable

4. Consider adding these as computed values from existing dashboard data:
   - If alunos data already fetched with frequencia, can compute in place
   - If not, add separate useQuery for frequency stats

5. Handle loading states (show skeleton or dash while computing)
6. Remove all TODO comments after implementation
  </action>
  <verify>
pnpm typecheck - no errors
grep "TODO.*frequencia" role-specific-dashboards.tsx - returns 0 matches
grep "TODO.*grade" role-specific-dashboards.tsx - returns 0 matches
grep "hardcoded\|FIXME" role-specific-dashboards.tsx - returns 0 matches
  </verify>
  <done>Role dashboards show real baixa frequencia count and grades from database</done>
</task>

<task type="auto">
  <name>Task 3: Implement PDF export in boletim page</name>
  <files>gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/boletim/page.tsx</files>
  <action>
Implement the TODO at line ~438 for PDF export:

1. Read boletim page to understand current structure
2. Install jspdf if not already installed: pnpm add jspdf @types/jspdf
3. Create PDF export function:
   ```typescript
   const handleExportPDF = async () => {
     const { jsPDF } = await import('jspdf')
     const doc = new jsPDF()

     // Add header
     doc.setFontSize(16)
     doc.text('Boletim Escolar', 20, 20)

     // Add student info
     doc.setFontSize(12)
     doc.text(`Aluno: ${student.nome}`, 20, 35)
     doc.text(`Turma: ${turma.nome}`, 20, 42)
     doc.text(`Ano Letivo: ${anoLetivo}`, 20, 49)

     // Add grades table
     // ... format grades as table

     // Save
     doc.save(`boletim-${student.nome.replace(/\s/g, '-')}.pdf`)
   }
   ```
4. Connect to existing "Exportar PDF" button
5. Handle loading state during PDF generation
6. Show toast on success: "PDF exportado com sucesso"
7. Remove the TODO comment

Follow existing PDF export patterns if any exist in the codebase (check lib/export/).
  </action>
  <verify>
pnpm typecheck - no errors
grep "TODO.*PDF" boletim/page.tsx - returns 0 matches
grep "jsPDF" boletim/page.tsx - import exists
  </verify>
  <done>Boletim page exports student grades as PDF document</done>
</task>

</tasks>

<verification>
1. pnpm typecheck passes
2. pnpm lint passes
3. grep -r "TODO" in modified files - no remaining TODOs
4. AbrirAulaWorkflow creates session successfully
5. Dashboards show real calculated values
6. PDF download works in boletim page
</verification>

<success_criteria>
- AbrirAulaWorkflow calls API and creates session record
- Role dashboards compute baixa frequencia from real frequency data
- Role dashboards compute grade from notas table
- Boletim page generates and downloads PDF
- No hardcoded placeholder values remain
- No TODO comments remain in these files
</success_criteria>

<output>
After completion, create `.planning/phases/15-technical-debt-cleanup/15-05-SUMMARY.md`
</output>
