---
phase: 15-technical-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/lib/api/dashboard-stats.ts
  - gestao_fronteira/app/(dashboard)/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard page fetches stats via API service, not direct Supabase"
    - "DashboardStatsApiService exists and follows VivenciasApiService pattern"
    - "pnpm typecheck passes with no new errors"
  artifacts:
    - path: "gestao_fronteira/lib/api/dashboard-stats.ts"
      provides: "Dashboard stats API service"
      exports: ["DashboardStatsApiService", "dashboardStatsApi"]
  key_links:
    - from: "gestao_fronteira/app/(dashboard)/dashboard/page.tsx"
      to: "lib/api/dashboard-stats.ts"
      via: "import dashboardStatsApi"
      pattern: "dashboardStatsApi\\.getStats"
---

<objective>
Create DashboardStatsApiService and migrate direct Supabase queries from dashboard page

Purpose: Enforce three-layer architecture (page -> API service -> Supabase) per STD-03
Output: Dashboard stats centralized in lib/api/dashboard-stats.ts, page uses service
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@gestao_fronteira/lib/api/base.ts
@gestao_fronteira/lib/api/vivencias.ts
@gestao_fronteira/app/(dashboard)/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DashboardStatsApiService</name>
  <files>gestao_fronteira/lib/api/dashboard-stats.ts</files>
  <action>
Create new API service following VivenciasApiService pattern:

1. Extend BaseApiService with entity name 'dashboard-stats'
2. Create interface DashboardStats with fields:
   - totalAlunos: number
   - totalEscolas: number
   - totalTurmas: number
   - totalUsuarios: number
   - totalMatriculas: number
   - frequenciaGeral: number
   - alunosAtivos: number
3. Create interface DashboardStatsOptions for filtering:
   - escolaId?: string (for escola-scoped data)
   - includeInactive?: boolean
4. Implement getStats(options?: DashboardStatsOptions) method:
   - Use Promise.all for parallel queries (7 queries from dashboard/page.tsx)
   - Apply escola filter when escolaId provided
   - Use supabase.from().select('id', { count: 'exact', head: true }) pattern
   - Use structured logger for errors
5. Export singleton: `export const dashboardStatsApi = new DashboardStatsApiService()`
6. Add JSDoc comments for all public methods

Follow exact patterns from lib/api/vivencias.ts for error handling and return types.
  </action>
  <verify>
pnpm typecheck - no errors in new file
grep "dashboardStatsApi" lib/api/dashboard-stats.ts - export exists
  </verify>
  <done>DashboardStatsApiService created with getStats method returning typed DashboardStats</done>
</task>

<task type="auto">
  <name>Task 2: Migrate dashboard page to use API service</name>
  <files>gestao_fronteira/app/(dashboard)/dashboard/page.tsx</files>
  <action>
Replace direct Supabase queries (lines ~104-130) with dashboardStatsApi:

1. Import at top: `import { dashboardStatsApi } from '@/lib/api/dashboard-stats'`
2. Replace the Promise.all block with:
   ```typescript
   const stats = await dashboardStatsApi.getStats({ escolaId: selectedEscolaId })
   ```
3. Update component props/state to use stats object:
   - Replace alunosResult.count with stats.totalAlunos
   - Replace escolasResult.count with stats.totalEscolas
   - etc.
4. Remove direct supabase import if no longer needed
5. Remove the 7 individual query variables
6. Keep existing error handling pattern (toast on error)

DO NOT change any UI rendering logic - only data fetching source.
  </action>
  <verify>
pnpm typecheck - no errors
pnpm lint - no errors
grep "supabase.from" app/(dashboard)/dashboard/page.tsx - should return NO matches (or only unrelated ones)
  </verify>
  <done>Dashboard page fetches all stats via dashboardStatsApi.getStats()</done>
</task>

</tasks>

<verification>
1. pnpm typecheck passes
2. pnpm lint passes
3. Dashboard page loads without errors (check browser console)
4. grep -r "supabase.from.*alunos.*count" app/(dashboard)/dashboard/ returns no direct queries
</verification>

<success_criteria>
- DashboardStatsApiService exists in lib/api/dashboard-stats.ts
- Dashboard page imports and uses dashboardStatsApi
- No direct Supabase queries remain in dashboard page.tsx
- All stats display correctly (same values as before)
- TypeScript types are properly defined
</success_criteria>

<output>
After completion, create `.planning/phases/15-technical-debt-cleanup/15-01-SUMMARY.md`
</output>
