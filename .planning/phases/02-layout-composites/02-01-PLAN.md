---
phase: 02-layout-composites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/components/layout/sidebar.tsx
  - gestao_fronteira/components/layout/header.tsx
  - gestao_fronteira/components/layout/index.ts
autonomous: true

must_haves:
  truths:
    - "User can navigate using sidebar with hierarchical structure"
    - "Sidebar collapses on mobile (hamburger menu visible)"
    - "User can search using global search field in header"
    - "User can see notification bell indicator in header"
    - "User can access user dropdown with quick actions"
  artifacts:
    - path: "gestao_fronteira/components/layout/sidebar.tsx"
      provides: "Hierarchical navigation with EDUCA styling"
      min_lines: 200
    - path: "gestao_fronteira/components/layout/header.tsx"
      provides: "Header with global search field"
      contains: "search"
  key_links:
    - from: "sidebar.tsx"
      to: "CSS variables"
      via: "Tailwind classes using EDUCA tokens"
      pattern: "rounded-educa|shadow-educa|font-display"
    - from: "header.tsx"
      to: "search input"
      via: "search-box element"
      pattern: "search|Search"
---

<objective>
Update Sidebar and Header layout components to follow EDUCA design system from mockups.

Purpose: Create the application shell that wraps all dashboard content, with consistent navigation and header patterns matching the HTML mockups.
Output: Updated Sidebar with EDUCA styling, Header with global search field, responsive behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summaries (tokens, components we'll use)
@.planning/phases/01-design-system-foundation/01-01-SUMMARY.md
@.planning/phases/01-design-system-foundation/01-02-SUMMARY.md

# HTML mockup reference for design patterns
@docs/educa-ui-mockups.html

# Existing layout components to update
@gestao_fronteira/components/layout/sidebar.tsx
@gestao_fronteira/components/layout/header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Sidebar with EDUCA design tokens</name>
  <files>gestao_fronteira/components/layout/sidebar.tsx</files>
  <action>
    Update the Sidebar component to use EDUCA design tokens from Phase 1:

    1. Replace background: `bg-white` → keep but add subtle gradient header per mockup
    2. Update nav-item styling:
       - Active state: `bg-green-50 text-green-600` (matches mockup `.nav-item.active`)
       - Hover state: `bg-gray-50`
       - Border radius: `rounded-[10px]` (matches mockup `border-radius: 10px`)
    3. Update group section titles:
       - Font size: `text-[0.7rem]` uppercase tracking-wide
       - Color: `text-gray-400`
    4. Update sidebar header:
       - Add EDUCA logo SVG or use existing MunicipalBrasao
       - Ensure Lexend font for "Sistema Escolar" title
    5. Keep existing navigation structure and role-based filtering
    6. Ensure collapse button uses EDUCA styling (rounded, subtle)

    Reference mockup CSS:
    - `.nav-item`: padding 12px, border-radius 10px, font-size 0.9rem, font-weight 500
    - `.nav-item.active`: background green-50, color green-600
    - `.nav-section-title`: font-size 0.7rem, uppercase, letter-spacing 0.5px

    DO NOT change the navigation items or role filtering logic.
  </action>
  <verify>
    npm run build passes
    Visual inspection: sidebar matches mockup styling
  </verify>
  <done>
    Sidebar uses EDUCA design tokens (green active states, proper border-radius, typography)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add global search field to Header</name>
  <files>gestao_fronteira/components/layout/header.tsx</files>
  <action>
    Add a global search field to the Header component (LAY-02 requirement):

    1. Add search box in header-right area, before notification bell:
       ```tsx
       <div className="flex items-center gap-2.5 px-4 py-2.5 bg-gray-50 border border-gray-200 rounded-[10px] w-[280px]">
         <Search className="h-[18px] w-[18px] text-gray-400" />
         <input
           type="text"
           placeholder="Buscar alunos, turmas..."
           className="border-none bg-transparent outline-none font-sans text-[0.9rem] text-gray-800 w-full placeholder:text-gray-400"
         />
       </div>
       ```

    2. Import Search icon from lucide-react

    3. Position search box between page title area and notification bell

    4. Style matches mockup `.search-box`:
       - padding: 10px 16px
       - background: gray-50
       - border: 1px solid gray-200
       - border-radius: 10px
       - width: 280px
       - Input: no border, transparent bg, font-family Inter, font-size 0.9rem

    5. Keep existing notification bell, user dropdown, and connection status

    Note: This is a visual placeholder for now. Search functionality will be wired in a future phase.
  </action>
  <verify>
    npm run build passes
    Header shows search field with magnifying glass icon
  </verify>
  <done>
    Header contains global search field matching mockup design (LAY-02 satisfied)
  </done>
</task>

<task type="auto">
  <name>Task 3: Ensure responsive layout behavior</name>
  <files>gestao_fronteira/components/layout/sidebar.tsx, gestao_fronteira/components/layout/header.tsx</files>
  <action>
    Verify and update responsive behavior for layout components (LAY-05 requirement):

    1. Sidebar responsive:
       - Desktop (1200+): Full sidebar visible (260px width)
       - Tablet (768-1199): Sidebar can be collapsed via button
       - Mobile (<768): Sidebar hidden by default, show hamburger menu in header

    2. Header responsive:
       - Desktop: Full header with search, notifications, user dropdown
       - Tablet: Search field may be narrower (200px)
       - Mobile: Hide search field, show only essential icons

    3. Add responsive classes:
       - Sidebar: `hidden md:flex` for desktop-only full sidebar
       - Search: `hidden lg:flex` or `w-[200px] lg:w-[280px]`
       - Header already has `hidden md:flex` - verify it works

    4. Verify MobileNav.tsx is still being used for mobile navigation
       - Check if it's imported in dashboard layout
       - Ensure it matches EDUCA styling

    DO NOT break existing mobile navigation functionality.
  </action>
  <verify>
    npm run build passes
    Resize browser: verify sidebar collapses, search field adapts
  </verify>
  <done>
    Layout responds correctly at desktop (1200+), tablet (768-1199), and mobile (<768) breakpoints
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run typecheck` passes
- [ ] Sidebar shows EDUCA styling (green active states, proper typography)
- [ ] Header shows global search field
- [ ] Responsive behavior works at all breakpoints
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- LAY-01 (sidebar navigation) verified
- LAY-02 (header search field) satisfied
- LAY-03 (notification bell) maintained
- LAY-04 (user dropdown) maintained
- LAY-05 (responsive grid) verified
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-composites/02-01-SUMMARY.md`
</output>
