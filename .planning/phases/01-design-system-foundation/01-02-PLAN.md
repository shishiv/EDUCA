---
phase: 01-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - gestao_fronteira/components/ui/button.tsx
  - gestao_fronteira/components/ui/card.tsx
  - gestao_fronteira/components/ui/input.tsx
  - gestao_fronteira/components/ui/avatar.tsx
  - gestao_fronteira/components/ui/badge.tsx
  - gestao_fronteira/components/ui/label.tsx
  - gestao_fronteira/components/ui/form-field.tsx
autonomous: true
must_haves:
  truths:
    - "Button component renders with primary/secondary/ghost variants"
    - "Card component has styled header and content areas"
    - "Input fields have visible focus states matching mockup"
    - "Avatar shows image with fallback to initials"
    - "Badge displays with EDUCA color variants"
    - "Form fields have labels visually and programmatically associated"
    - "Error messages display below fields, not just red border"
  artifacts:
    - path: "gestao_fronteira/components/ui/button.tsx"
      provides: "Button with EDUCA variants"
      exports: ["Button", "buttonVariants"]
    - path: "gestao_fronteira/components/ui/card.tsx"
      provides: "Card with header/content styling"
      exports: ["Card", "CardHeader", "CardContent"]
    - path: "gestao_fronteira/components/ui/input.tsx"
      provides: "Input with EDUCA focus states"
      exports: ["Input"]
    - path: "gestao_fronteira/components/ui/form-field.tsx"
      provides: "Form field wrapper with label and error support"
      exports: ["FormField"]
      min_lines: 40
  key_links:
    - from: "button.tsx"
      to: "globals.css"
      via: "CSS variables for colors"
      pattern: "(bg-educa|bg-primary)"
    - from: "form-field.tsx"
      to: "label.tsx"
      via: "htmlFor association"
      pattern: "htmlFor"
---

<objective>
Update primitive UI components to match EDUCA design system mockups.

Purpose: Establish consistent visual language across all UI elements by updating shadcn/ui components to use EDUCA design tokens. Ensure accessibility with proper label association and error message display.

Output: Updated Button, Card, Input, Avatar, Badge, Label components plus new FormField wrapper for accessibility.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-foundation/01-01-SUMMARY.md

# Design reference
@docs/educa-brand-guidelines.html
@docs/educa-ui-mockups.html

# Current components
@gestao_fronteira/components/ui/button.tsx
@gestao_fronteira/components/ui/card.tsx
@gestao_fronteira/components/ui/input.tsx
@gestao_fronteira/components/ui/avatar.tsx
@gestao_fronteira/components/ui/badge.tsx
@gestao_fronteira/components/ui/label.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Button with EDUCA variants</name>
  <files>gestao_fronteira/components/ui/button.tsx</files>
  <action>
    Update buttonVariants to match EDUCA design system (from brand-guidelines mockup):

    **Update variants object:**
    ```typescript
    variants: {
      variant: {
        // Primary - Green gradient like mockup buttons
        default: 'bg-gradient-to-r from-green-600 to-green-500 text-white hover:from-green-700 hover:to-green-600 shadow-sm',

        // Secondary - Outlined with green border
        secondary: 'border-2 border-green-600 text-green-700 bg-white hover:bg-green-50',

        // Ghost - Minimal, just text
        ghost: 'text-gray-600 hover:text-green-600 hover:bg-green-50',

        // Destructive - Keep existing, already good
        destructive: 'bg-destructive text-destructive-foreground hover:bg-destructive/90',

        // Outline - Gray border variant
        outline: 'border border-gray-300 bg-white text-gray-700 hover:bg-gray-50 hover:border-gray-400',

        // Link - Text link style
        link: 'text-green-600 underline-offset-4 hover:underline',
      },
      size: {
        default: 'h-10 px-4 py-2 text-sm',
        sm: 'h-9 px-3 text-sm',
        lg: 'h-12 px-6 text-base',
        icon: 'h-10 w-10',
        // Touch-friendly size for mobile (44px minimum per WCAG)
        touch: 'h-11 min-w-[44px] px-4 text-base',
      },
    }
    ```

    **Update base styles:**
    - Use `rounded-educa` (8px) instead of `rounded-md`
    - Add `font-medium` for button text weight
    - Ensure focus ring uses `ring-green-500/50`

    AVOID: Changing the component API or props interface.
    WHY: Existing code depends on current API - only update styles.
  </action>
  <verify>
    - pnpm typecheck passes
    - Button with variant="default" shows green gradient
    - Button with variant="secondary" shows outlined style
    - Button with variant="ghost" shows minimal style
  </verify>
  <done>
    - Button renders with EDUCA styling for all variants
    - Touch-friendly size option available
    - Requirement COMP-01 satisfied
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Card with header/content styling</name>
  <files>gestao_fronteira/components/ui/card.tsx</files>
  <action>
    Update Card components to match mockup card styling:

    **Card (container):**
    - `rounded-educa-md` (12px)
    - `shadow-educa` for subtle shadow
    - `bg-white` with `border border-gray-200`
    - Hover state: `hover:shadow-educa-md` for interactive cards

    **CardHeader:**
    - `px-6 pt-6 pb-4` for consistent padding
    - Support optional gradient background for module cards:
      Add prop `gradient?: 'turmas' | 'alunos' | 'frequencia' | ...`
      When set, apply `bg-gradient-to-r from-{module}-500/10 to-transparent`

    **CardTitle:**
    - `font-display text-h3` (use Lexend, 18px)
    - `text-gray-900` for dark text

    **CardDescription:**
    - `text-small text-gray-500`

    **CardContent:**
    - `px-6 pb-6` padding
    - `pt-0` when following CardHeader

    **CardFooter:**
    - `px-6 py-4 border-t border-gray-100`
    - `bg-gray-50/50` subtle background

    AVOID: Breaking existing Card usage patterns.
    WHY: Card is used throughout app - maintain backward compatibility.
  </action>
  <verify>
    - Card renders with 12px border-radius
    - CardHeader has correct padding
    - CardTitle uses Lexend font
  </verify>
  <done>
    - Card styled per EDUCA mockups
    - Header/content areas visually distinct
    - Requirement COMP-02 satisfied
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Input with focus states</name>
  <files>gestao_fronteira/components/ui/input.tsx</files>
  <action>
    Update Input to match mockup form styling with visible focus states:

    **Base styles:**
    ```typescript
    'flex h-10 w-full rounded-educa border border-gray-300 bg-white px-3 py-2 text-body text-gray-900',
    'placeholder:text-gray-400',
    'transition-colors duration-150',
    ```

    **Focus states (visible per ACESS-02):**
    ```typescript
    'focus:outline-none focus:ring-2 focus:ring-green-500/30 focus:border-green-500',
    'focus-visible:ring-2 focus-visible:ring-green-500/30 focus-visible:border-green-500',
    ```

    **Disabled state:**
    ```typescript
    'disabled:cursor-not-allowed disabled:bg-gray-100 disabled:text-gray-400',
    ```

    **Error state (add variant support):**
    Add optional `error` prop:
    ```typescript
    interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {
      error?: boolean;
    }

    // When error=true:
    'border-red-500 focus:ring-red-500/30 focus:border-red-500'
    ```

    The error styling is for ACESS-03 - but the message itself comes from FormField wrapper.

    AVOID: Changing the base component structure.
    WHY: Input is heavily used - keep API stable.
  </action>
  <verify>
    - Input shows green ring on focus
    - Input with error prop shows red border
    - Focus ring is clearly visible (contrast check)
  </verify>
  <done>
    - Input has visible focus states
    - Error state variant available
    - Requirement COMP-03 satisfied
  </done>
</task>

<task type="auto">
  <name>Task 4: Update Avatar with fallback styling</name>
  <files>gestao_fronteira/components/ui/avatar.tsx</files>
  <action>
    Update Avatar components to match EDUCA styling:

    **Avatar (container):**
    - Default size: `h-10 w-10`
    - Add size variants: `sm` (h-8), `md` (h-10), `lg` (h-12), `xl` (h-16)
    - `rounded-full` for circular shape
    - `ring-2 ring-white` for subtle border effect

    **AvatarImage:**
    - `object-cover` to prevent distortion
    - `aspect-square` for consistent sizing

    **AvatarFallback:**
    - `bg-gradient-to-br from-green-500 to-blue-500` gradient background
    - `text-white font-medium` for initials
    - Font size scaled to avatar size

    **Add size prop to Avatar:**
    ```typescript
    interface AvatarProps {
      size?: 'sm' | 'md' | 'lg' | 'xl';
    }

    const sizeClasses = {
      sm: 'h-8 w-8 text-xs',
      md: 'h-10 w-10 text-sm',
      lg: 'h-12 w-12 text-base',
      xl: 'h-16 w-16 text-lg',
    }
    ```

    AVOID: Changing AvatarImage/AvatarFallback composition pattern.
    WHY: Radix UI pattern is well-established.
  </action>
  <verify>
    - Avatar shows image when available
    - Avatar shows gradient fallback with initials when no image
    - Size variants work correctly
  </verify>
  <done>
    - Avatar styled per EDUCA design
    - Size variants available
    - Fallback shows gradient with initials
    - Requirement COMP-04 satisfied
  </done>
</task>

<task type="auto">
  <name>Task 5: Update Badge with EDUCA variants</name>
  <files>gestao_fronteira/components/ui/badge.tsx</files>
  <action>
    Update Badge variants to include EDUCA module colors:

    **Update badgeVariants:**
    ```typescript
    variants: {
      variant: {
        // Existing
        default: 'bg-primary text-primary-foreground',
        secondary: 'bg-gray-100 text-gray-700 border border-gray-200',
        destructive: 'bg-red-100 text-red-700 border border-red-200',
        outline: 'border border-gray-300 text-gray-700',

        // EDUCA semantic
        success: 'bg-green-100 text-green-700 border border-green-200',
        warning: 'bg-yellow-100 text-yellow-800 border border-yellow-200',
        info: 'bg-blue-100 text-blue-700 border border-blue-200',

        // Module badges
        alunos: 'bg-violet-100 text-violet-700 border border-violet-200',
        turmas: 'bg-sky-100 text-sky-700 border border-sky-200',
        frequencia: 'bg-emerald-100 text-emerald-700 border border-emerald-200',
        notas: 'bg-orange-100 text-orange-700 border border-orange-200',

        // BNCC Campos (for Diario Infantil)
        'campo-eu': 'bg-campo-eu-light text-pink-700 border border-pink-200',
        'campo-corpo': 'bg-campo-corpo-light text-orange-700 border border-orange-200',
        'campo-tracos': 'bg-campo-tracos-light text-violet-700 border border-violet-200',
        'campo-escuta': 'bg-campo-escuta-light text-sky-700 border border-sky-200',
        'campo-espacos': 'bg-campo-espacos-light text-emerald-700 border border-emerald-200',
      }
    }
    ```

    **Update base styles:**
    - `rounded-full` for pill shape
    - `px-2.5 py-0.5` for comfortable padding
    - `text-xs font-medium` for badge text
    - `inline-flex items-center`

    AVOID: Removing existing variant names.
    WHY: Existing code may use default/secondary/destructive.
  </action>
  <verify>
    - Badge with variant="success" shows green styling
    - Badge with variant="campo-eu" shows pink styling
    - All variants render correctly
  </verify>
  <done>
    - Badge has EDUCA semantic variants
    - Badge has module color variants
    - Badge has BNCC Campos variants
    - Requirement COMP-05 satisfied
  </done>
</task>

<task type="auto">
  <name>Task 6: Create FormField wrapper for accessibility</name>
  <files>gestao_fronteira/components/ui/form-field.tsx</files>
  <action>
    Create a new FormField component that wraps inputs with proper label association and error message display.

    This addresses ACESS-02 (labels associated) and ACESS-03 (error messages below fields).

    ```typescript
    'use client'

    import * as React from 'react'
    import { cn } from '@/lib/utils'
    import { Label } from './label'

    export interface FormFieldProps {
      label: string;
      htmlFor: string;
      error?: string;
      description?: string;
      required?: boolean;
      children: React.ReactNode;
      className?: string;
    }

    export function FormField({
      label,
      htmlFor,
      error,
      description,
      required,
      children,
      className,
    }: FormFieldProps) {
      return (
        <div className={cn('space-y-2', className)}>
          <Label
            htmlFor={htmlFor}
            className={cn(
              'text-sm font-medium text-gray-700',
              error && 'text-red-600'
            )}
          >
            {label}
            {required && <span className="text-red-500 ml-1">*</span>}
          </Label>

          {children}

          {description && !error && (
            <p className="text-sm text-gray-500" id={`${htmlFor}-description`}>
              {description}
            </p>
          )}

          {error && (
            <p
              className="text-sm text-red-600"
              id={`${htmlFor}-error`}
              role="alert"
            >
              {error}
            </p>
          )}
        </div>
      )
    }
    ```

    **Key accessibility features:**
    - `htmlFor` associates label with input (ACESS-02)
    - Error message displayed below field, not just red border (ACESS-03)
    - `role="alert"` on error for screen readers
    - `aria-describedby` should be added by consumer to link input with error

    **Export from index.ts:**
    Add `export { FormField } from './form-field'` to components/ui/index.ts

    AVOID: Replacing existing form handling (react-hook-form integration exists).
    WHY: This is a simple wrapper - existing form logic should continue to work.
  </action>
  <verify>
    - FormField renders label above input
    - Error message appears below input when error prop is set
    - Label is programmatically associated via htmlFor
  </verify>
  <done>
    - FormField wrapper created
    - Labels associated with fields (ACESS-02)
    - Error messages display below fields (ACESS-03)
    - Requirement COMP-06 satisfied
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds without errors
- [ ] `pnpm typecheck` passes
- [ ] Button renders green gradient on primary variant
- [ ] Card has 12px border radius
- [ ] Input shows visible green focus ring
- [ ] Avatar shows gradient fallback with initials
- [ ] Badge variants include campo-eu (pink)
- [ ] FormField displays error message below input
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Components use EDUCA design tokens
- Accessibility requirements ACESS-02 and ACESS-03 addressed
- Requirements COMP-01 through COMP-06 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-02-SUMMARY.md`
</output>
