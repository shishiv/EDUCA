---
phase: 20-ui-ux-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/app/providers.tsx
  - gestao_fronteira/components/diary/ClassDiaryDetail.tsx
autonomous: true

must_haves:
  truths:
    - "Toasts appear only once when triggered"
    - "Dialog close button appears only once in ClassDiaryDetail"
  artifacts:
    - path: "gestao_fronteira/app/providers.tsx"
      provides: "Root providers without duplicate Toaster"
      contains: "QueryClientProvider"
      not_contains: "Toaster"
    - path: "gestao_fronteira/components/diary/ClassDiaryDetail.tsx"
      provides: "Class diary detail dialog"
      not_contains: "DialogClose asChild"
  key_links:
    - from: "gestao_fronteira/app/(dashboard)/layout.tsx"
      to: "@/components/ui/sonner"
      via: "Toaster component import"
      pattern: "Toaster.*from.*components/ui/sonner"
---

<objective>
Remove duplicate UI elements causing visual inconsistencies.

Purpose: Fix UIX-01 (duplicate sonner toasts) and UIX-02 (double close buttons)
Output: Clean UI with single toast instance and single dialog close button
</objective>

<execution_context>
@C:\Users\shiv\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\shiv\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-ui-ux-fixes/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate Toaster from providers.tsx</name>
  <files>gestao_fronteira/app/providers.tsx</files>
  <action>
Remove the Toaster component from providers.tsx:

1. Delete the import statement for Toaster from 'sonner' (line 5)
2. Delete the mounted state and useEffect that tracks client-side mount (lines 9-14)
3. Delete the conditional Toaster render `{mounted && <Toaster ... />}` (line 35)

The dashboard layout already has the styled Toaster from @/components/ui/sonner with proper theming, position (bottom-right), and accessibility features. Keeping only one Toaster prevents duplicate toast notifications.

Result: providers.tsx contains only QueryClientProvider and ServiceWorkerProvider.
  </action>
  <verify>
1. `grep -n "Toaster" gestao_fronteira/app/providers.tsx` returns no matches
2. `grep -n "mounted" gestao_fronteira/app/providers.tsx` returns no matches
3. `pnpm.cmd typecheck` passes (no type errors from removal)
  </verify>
  <done>
Toaster import and render removed from providers.tsx. Only the styled Toaster in dashboard layout remains.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate close button from ClassDiaryDetail</name>
  <files>gestao_fronteira/components/diary/ClassDiaryDetail.tsx</files>
  <action>
Remove the manual DialogClose wrapper around the close button in ClassDiaryDetail.tsx:

1. Remove the import for DialogClose from the import statement (line 26)
2. Remove the import for X icon from lucide-react (line 41) - no longer needed since DialogContent provides its own X
3. Delete lines 108-112 that render the duplicate close button:
   ```tsx
   <DialogClose asChild>
     <Button variant="ghost" size="icon">
       <X className="h-4 w-4" />
     </Button>
   </DialogClose>
   ```
4. Simplify the DialogTitle to just contain the icon and text without the flex justify-between wrapper:
   ```tsx
   <DialogTitle className="flex items-center gap-2">
     <BookOpen className="h-5 w-5" />
     Detalhes da Aula
   </DialogTitle>
   ```

The DialogContent component from @/components/ui/dialog.tsx (lines 47-50) already renders a close button with the X icon at `absolute right-4 top-4`. Adding another creates visual clutter and accessibility confusion.
  </action>
  <verify>
1. `grep -n "DialogClose" gestao_fronteira/components/diary/ClassDiaryDetail.tsx` returns no matches
2. `grep -c "X," gestao_fronteira/components/diary/ClassDiaryDetail.tsx` returns 0 (X icon no longer imported for close button)
3. `pnpm.cmd typecheck` passes (no type errors from removal)
  </verify>
  <done>
Manual DialogClose removed. Dialog now has single close button from DialogContent.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `cd gestao_fronteira && pnpm.cmd build` exits with code 0
2. TypeScript passes: `pnpm.cmd typecheck` shows no new errors
3. Toaster grep: Only dashboard layout.tsx should import Toaster
4. DialogClose grep: ClassDiaryDetail.tsx should not use DialogClose
</verification>

<success_criteria>
- UIX-01: Single Toaster in dashboard layout only
- UIX-02: Single close button (X) in ClassDiaryDetail dialog
- Build and typecheck pass without errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-ui-ux-fixes/20-01-SUMMARY.md`
</output>
