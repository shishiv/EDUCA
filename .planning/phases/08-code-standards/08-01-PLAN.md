---
phase: 08-code-standards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/CONVENTIONS.md
  - gestao_fronteira/app/(dashboard)/relatorios/conteudo/page.tsx
autonomous: true

must_haves:
  truths:
    - "CONVENTIONS.md documents data fetching pattern with React Query + API layer"
    - "CONVENTIONS.md documents filter standard with 'todos' as default"
    - "All filter options in codebase use 'todos'/'todas' (not 'all')"
  artifacts:
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "Data fetching and filter conventions"
      contains: "## Data Fetching Pattern"
    - path: "gestao_fronteira/app/(dashboard)/relatorios/conteudo/page.tsx"
      provides: "Fixed filter default"
      contains: "value: 'todas'"
  key_links:
    - from: "CONVENTIONS.md"
      to: "VivenciasApiService pattern"
      via: "example reference"
      pattern: "VivenciasApiService|lib/api/vivencias"
---

<objective>
Document data fetching and filter patterns in CONVENTIONS.md and fix the filter inconsistency.

Purpose: Establish documented standards (STD-01, STD-02) that guide future development and migration work.
Output: Updated CONVENTIONS.md with patterns + consistent filter defaults across codebase.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/08-code-standards/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document data fetching pattern in CONVENTIONS.md</name>
  <files>.planning/codebase/CONVENTIONS.md</files>
  <action>
Add a new section "## Data Fetching Pattern" after the "## API Service Pattern" section in CONVENTIONS.md.

Document the three-layer pattern:
1. **API Service Layer** (lib/api/*.ts)
   - Reference VivenciasApiService as the exemplar
   - Class extends BaseApiService
   - Uses logger.error for errors, logger.info for success
   - Returns typed data, handles PGRST116 gracefully
   - Export singleton instance

2. **React Query Layer** (hooks/use-*-query.ts)
   - Query keys from lib/react-query.ts
   - staleTime guidelines: 5min for static data, 2min for moderate, 1min for active
   - Use logger for error logging

3. **Page Component Layer**
   - Use React Query hooks
   - Handle loading/error states
   - Client-side filtering on cached data

Include code examples from:
- lib/api/vivencias.ts (service pattern)
- hooks/use-diary-query.ts (hook pattern)

Add a decision table:
| Data Type | Where to Add Method | staleTime |
|-----------|---------------------|-----------|
| List pages | Existing *ApiService | 5 min |
| Detail pages | Existing *ApiService | 2 min |
| Active sessions | New hook if none exists | 1 min |
  </action>
  <verify>Read CONVENTIONS.md and confirm "## Data Fetching Pattern" section exists with three-layer pattern documented</verify>
  <done>CONVENTIONS.md contains data fetching pattern documentation with examples and decision table</done>
</task>

<task type="auto">
  <name>Task 2: Document filter standard and fix inconsistency</name>
  <files>.planning/codebase/CONVENTIONS.md, gestao_fronteira/app/(dashboard)/relatorios/conteudo/page.tsx</files>
  <action>
**Part A: Document filter standard in CONVENTIONS.md**

Add a new section "## Filter Standard" after the "## Data Fetching Pattern" section:

```markdown
## Filter Standard

**Default value:** `'todos'` (masculine) or `'todas'` (feminine noun context)

**Never use:** `'all'` (English) - use Portuguese for consistency

**Pattern:**
```typescript
// State initialization
const [statusFilter, setStatusFilter] = useState('todos')

// Filter options
const filterOptions = [
  { value: 'todos', label: 'Todos os Status' }, // or 'Todos' for short
  { value: 'ativo', label: 'Ativos' },
  // ...
]

// Feminine context (escolas, turmas, disciplinas)
const [escolaFilter, setEscolaFilter] = useState('todas')
const escolaOptions = [
  { value: 'todas', label: 'Todas as Escolas' },
  // ...
]

// Reset behavior
const clearFilters = () => {
  setStatusFilter('todos')
  setEscolaFilter('todas')
}
```

**Filtering logic:**
- Filter on client-side after fetching full list
- Use `.filter()` with condition: `filter === 'todos' || item.status === filter`
```

**Part B: Fix the inconsistency**

In `gestao_fronteira/app/(dashboard)/relatorios/conteudo/page.tsx`:
- Find line ~122: `{ value: 'all', label: 'Todas as Disciplinas' }`
- Change to: `{ value: 'todas', label: 'Todas as Disciplinas' }`
- Also update the state initialization and filtering logic to use 'todas' instead of 'all'
  </action>
  <verify>
1. Read CONVENTIONS.md and confirm "## Filter Standard" section exists
2. Run: `grep -n "'all'" gestao_fronteira/app/(dashboard)/relatorios/conteudo/page.tsx` - should return empty
3. Run: `grep -n "'todas'" gestao_fronteira/app/(dashboard)/relatorios/conteudo/page.tsx` - should show the filter option
  </verify>
  <done>CONVENTIONS.md documents filter standard with 'todos'/'todas' default, and conteudo/page.tsx uses 'todas' (not 'all')</done>
</task>

</tasks>

<verification>
- [ ] CONVENTIONS.md has "## Data Fetching Pattern" section
- [ ] CONVENTIONS.md has "## Filter Standard" section
- [ ] `grep "'all'" gestao_fronteira/` returns no filter-related matches
- [ ] `pnpm typecheck` passes
</verification>

<success_criteria>
- STD-01 partial: Data fetching pattern documented
- STD-02 complete: Filter standard documented and codebase consistent with 'todos'/'todas'
</success_criteria>

<output>
After completion, create `.planning/phases/08-code-standards/08-01-SUMMARY.md`
</output>
