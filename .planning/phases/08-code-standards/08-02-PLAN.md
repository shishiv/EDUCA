---
phase: 08-code-standards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/lib/api/multi-guardian.ts
  - gestao_fronteira/lib/api/inep-integration.ts
  - gestao_fronteira/lib/api/enhanced-attendance.ts
  - gestao_fronteira/lib/api/rate-limiting.ts
  - gestao_fronteira/lib/services/attendance-immutability.ts
  - gestao_fronteira/lib/services/attendance-bulk-operations.ts
  - gestao_fronteira/lib/utils/export.ts
  - gestao_fronteira/lib/realtime/aulas-abertas-listener.ts
  - gestao_fronteira/lib/hooks/use-realtime-attendance.ts
  - gestao_fronteira/lib/hooks/use-attendance-history.ts
autonomous: true

must_haves:
  truths:
    - "All lib/api/ files use logger.error instead of console.error"
    - "All lib/services/ files use logger.error instead of console.error"
    - "All lib/utils/ files use logger.error instead of console.error"
    - "All lib/realtime/ files use logger.error instead of console.error"
    - "All lib/hooks/ files use logger.error instead of console.error"
  artifacts:
    - path: "gestao_fronteira/lib/api/multi-guardian.ts"
      provides: "Guardian API with logger"
      contains: "logger.error"
    - path: "gestao_fronteira/lib/api/inep-integration.ts"
      provides: "INEP API with logger"
      contains: "logger.error"
    - path: "gestao_fronteira/lib/realtime/aulas-abertas-listener.ts"
      provides: "Realtime listener with logger"
      contains: "logger.error"
  key_links:
    - from: "lib/api/*.ts"
      to: "lib/logger.ts"
      via: "import"
      pattern: "import.*logger.*from.*@/lib/logger"
---

<objective>
Migrate console.error to logger.error in all lib/ files (api, services, utils, realtime, hooks).

Purpose: Establish structured logging (STD-04) in the infrastructure layer before migrating pages.
Output: All lib/ files use logger with contextual metadata.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-code-standards/08-RESEARCH.md
@gestao_fronteira/lib/logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate logger in lib/api/ files (26 console.error calls)</name>
  <files>
    gestao_fronteira/lib/api/multi-guardian.ts,
    gestao_fronteira/lib/api/inep-integration.ts,
    gestao_fronteira/lib/api/enhanced-attendance.ts,
    gestao_fronteira/lib/api/rate-limiting.ts
  </files>
  <action>
For each file, replace console.error with logger.error following this pattern:

```typescript
// BEFORE
console.error('Error fetching guardians:', err)

// AFTER
import { logger } from '@/lib/logger'  // Add if not present

logger.error('Error fetching guardians', err, {
  feature: 'multi-guardian',  // Based on file name
  action: 'fetch_guardians'   // Based on function name
})
```

**File-specific context:**

1. **multi-guardian.ts** (12 calls):
   - feature: 'multi-guardian'
   - actions: fetch_guardians, create_guardian, update_guardian, etc.

2. **inep-integration.ts** (11 calls):
   - feature: 'inep-integration'
   - actions: export_data, validate_fields, submit_census, etc.

3. **enhanced-attendance.ts** (3 calls):
   - feature: 'attendance'
   - actions: create_session, bulk_update, lock_session

4. **rate-limiting.ts** (1 call):
   - feature: 'rate-limiting'
   - action: check_rate_limit

Ensure logger import is at the top with other imports. Do NOT modify any logic, only replace console.error calls.
  </action>
  <verify>
Run: `grep -c "console.error" gestao_fronteira/lib/api/multi-guardian.ts gestao_fronteira/lib/api/inep-integration.ts gestao_fronteira/lib/api/enhanced-attendance.ts gestao_fronteira/lib/api/rate-limiting.ts`
All counts should be 0.
  </verify>
  <done>lib/api/ files (multi-guardian, inep-integration, enhanced-attendance, rate-limiting) use logger.error with context metadata</done>
</task>

<task type="auto">
  <name>Task 2: Migrate logger in lib/services/, lib/utils/, lib/realtime/, lib/hooks/</name>
  <files>
    gestao_fronteira/lib/services/attendance-immutability.ts,
    gestao_fronteira/lib/services/attendance-bulk-operations.ts,
    gestao_fronteira/lib/utils/export.ts,
    gestao_fronteira/lib/realtime/aulas-abertas-listener.ts,
    gestao_fronteira/lib/hooks/use-realtime-attendance.ts,
    gestao_fronteira/lib/hooks/use-attendance-history.ts
  </files>
  <action>
For each file, replace console.error with logger.error:

**File-specific context:**

1. **attendance-immutability.ts** (2 calls):
   - feature: 'attendance-compliance'
   - actions: check_immutability, validate_lock

2. **attendance-bulk-operations.ts** (1 call):
   - feature: 'attendance'
   - action: bulk_operation

3. **export.ts** (4 calls):
   - feature: 'export'
   - actions: generate_csv, generate_pdf, download_file

4. **aulas-abertas-listener.ts** (5 calls):
   - feature: 'realtime'
   - actions: subscribe_sessions, handle_insert, handle_update, cleanup

5. **use-realtime-attendance.ts** (1 call):
   - feature: 'realtime'
   - action: attendance_subscription

6. **use-attendance-history.ts** (1 call):
   - feature: 'attendance'
   - action: fetch_history

Follow the same pattern as Task 1. Ensure logger import is present.
  </action>
  <verify>
Run: `grep -c "console.error" gestao_fronteira/lib/services/ gestao_fronteira/lib/utils/export.ts gestao_fronteira/lib/realtime/ gestao_fronteira/lib/hooks/use-realtime-attendance.ts gestao_fronteira/lib/hooks/use-attendance-history.ts`
All counts should be 0 or file not found (if grep doesn't find matches).
  </verify>
  <done>lib/services/, lib/utils/, lib/realtime/, lib/hooks/ files use logger.error with context metadata</done>
</task>

</tasks>

<verification>
- [ ] `grep -r "console.error" gestao_fronteira/lib/api/ | grep -v scripts | wc -l` returns low count (only expected in base error handler)
- [ ] `grep -r "console.error" gestao_fronteira/lib/services/ | wc -l` returns 0
- [ ] `grep -r "console.error" gestao_fronteira/lib/realtime/ | wc -l` returns 0
- [ ] `grep -r "console.error" gestao_fronteira/lib/hooks/ | wc -l` returns 0
- [ ] `pnpm typecheck` passes
</verification>

<success_criteria>
- STD-04 partial: ~27 console.error calls in lib/ migrated to logger.error
- All lib/ infrastructure uses structured logging with context
</success_criteria>

<output>
After completion, create `.planning/phases/08-code-standards/08-02-SUMMARY.md`
</output>
