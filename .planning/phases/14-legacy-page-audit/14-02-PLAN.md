---
phase: 14-legacy-page-audit
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - gestao_fronteira/app/showcase/page.tsx (DELETE)
  - gestao_fronteira/app/platform-names/page.tsx (DELETE)
  - gestao_fronteira/components/layout/sidebar.tsx
autonomous: true

must_haves:
  truths:
    - "Development pages removed from codebase"
    - "Sidebar only shows pages that are functional"
    - "Build passes after cleanup"
  artifacts:
    - path: "gestao_fronteira/components/layout/sidebar.tsx"
      provides: "Updated navigation reflecting audit decisions"
      contains: "navigationGroups"
  key_links:
    - from: "sidebar.tsx"
      to: "/dashboard/* pages"
      via: "href in navigation items"
      pattern: "href.*dashboard"
---

<objective>
Execute cleanup actions from the page audit: remove development pages and update sidebar navigation.

Purpose: Clean up the codebase by removing dev-only pages identified in audit and ensuring sidebar navigation reflects only functional pages.

Output: Cleaner codebase with no dev artifacts, sidebar aligned with functional pages
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/PAGE-AUDIT.md
@gestao_fronteira/components/layout/sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove development pages</name>
  <files>
gestao_fronteira/app/showcase/page.tsx
gestao_fronteira/app/platform-names/page.tsx
  </files>
  <action>
Delete the following development/testing pages that should not be in production:

1. Delete `gestao_fronteira/app/showcase/` directory
   - This was a component testing page
   - Command: `rm -rf gestao_fronteira/app/showcase`

2. Delete `gestao_fronteira/app/platform-names/` directory
   - This was a branding exploration page
   - Command: `rm -rf gestao_fronteira/app/platform-names`

After deletion, verify no imports or links reference these pages:
- `grep -r "showcase" gestao_fronteira/` should find nothing
- `grep -r "platform-names" gestao_fronteira/` should find nothing
  </action>
  <verify>
`ls gestao_fronteira/app/showcase 2>&1` returns "No such file or directory"
`ls gestao_fronteira/app/platform-names 2>&1` returns "No such file or directory"
`pnpm build` passes (no broken imports)
  </verify>
  <done>Development pages removed from codebase</done>
</task>

<task type="auto">
  <name>Task 2: Add Responsaveis to sidebar navigation</name>
  <files>gestao_fronteira/components/layout/sidebar.tsx</files>
  <action>
Based on audit findings, Responsaveis is a functional page that should be accessible.

Add Responsaveis to the Cadastros group in sidebar.tsx:

```typescript
// In the Cadastros group items array, after Matriculas:
{
  name: 'Responsaveis',
  href: '/dashboard/responsaveis',
  icon: Users,  // or appropriate icon
  roles: ['admin', 'diretor', 'secretario'],
},
```

Import the icon if needed (Users is already imported).

Decision rationale:
- Responsaveis page exists and is functional (real Supabase data)
- Links parents/guardians to students - core educational data
- Matches role pattern of other Cadastros items

Do NOT add Calendario or Sessoes to sidebar:
- These are admin-internal tools, not user-facing features
- Can be accessed via direct URL if needed
- Adding would clutter navigation
  </action>
  <verify>
`grep -A2 "Responsaveis" gestao_fronteira/components/layout/sidebar.tsx` shows the nav item.
`pnpm build` passes.
  </verify>
  <done>Responsaveis added to sidebar Cadastros group</done>
</task>

<task type="auto">
  <name>Task 3: Verify build and update STATE.md</name>
  <files>.planning/STATE.md</files>
  <action>
1. Run build to verify no regressions:
   `cd gestao_fronteira && pnpm build`

2. Update STATE.md:
   - Current Position: Phase 14 of 14 (Legacy Page Audit) - COMPLETE
   - Last activity: [date] - Completed 14-02-PLAN.md (Page Cleanup)
   - Progress: 100% (27/27 requirements)
   - Next action: v2.0 milestone complete, ready for deployment

3. Add decision to Decisions table:
   | 14-02 | Add Responsaveis to sidebar, keep Calendario/Sessoes hidden | Responsaveis is user-facing; others are admin tools |
  </action>
  <verify>
`pnpm build` exits with code 0.
`grep "Phase 14" .planning/STATE.md` shows COMPLETE status.
  </verify>
  <done>Build passes, state updated, phase complete</done>
</task>

</tasks>

<verification>
- Development pages deleted (showcase, platform-names)
- No broken imports or references
- Responsaveis visible in sidebar for admin/diretor/secretario
- Build passes without errors
- STATE.md updated with phase completion
</verification>

<success_criteria>
- Codebase free of development-only pages
- Sidebar reflects audit decisions (Responsaveis added)
- Build and typecheck pass
- Phase 14 marked complete in STATE.md
- v2.0 milestone complete (all 27 requirements satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/14-legacy-page-audit/14-02-SUMMARY.md`
</output>
