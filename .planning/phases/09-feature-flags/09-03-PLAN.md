---
phase: 09-feature-flags
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - gestao_fronteira/app/(dashboard)/dashboard/flags/page.tsx
autonomous: false

must_haves:
  truths:
    - "Admin can see all feature flags with escola toggles"
    - "Admin can bulk toggle a flag for multiple escolas"
    - "Toggle changes persist after page refresh"
    - "Only admin/gestor_sme can access the flags page"
  artifacts:
    - path: "gestao_fronteira/app/(dashboard)/dashboard/flags/page.tsx"
      provides: "Admin UI for flag management"
      min_lines: 150
  key_links:
    - from: "app/(dashboard)/dashboard/flags/page.tsx"
      to: "hooks/use-feature-flag.ts"
      via: "useFeatureFlagsWithStatus, useToggleFlags"
      pattern: "useFeatureFlagsWithStatus|useToggleFlags"
    - from: "app/(dashboard)/dashboard/flags/page.tsx"
      to: "lib/api/feature-flags.ts"
      via: "React Query mutation"
---

<objective>
Create admin UI for managing feature flags per escola with bulk toggle capability.

Purpose: Enable super admin (secretaria) to control module rollout for individual escolas.
Output: /dashboard/flags page with flag list, escola toggles, and bulk operations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-feature-flags/09-RESEARCH.md
@.planning/phases/09-feature-flags/09-02-SUMMARY.md
@gestao_fronteira/components/admin/users/user-list.tsx
@gestao_fronteira/hooks/use-feature-flag.ts
@gestao_fronteira/lib/api/feature-flags.ts
@gestao_fronteira/types/feature-flags.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create flags admin page</name>
  <files>gestao_fronteira/app/(dashboard)/dashboard/flags/page.tsx</files>
  <action>
Create admin page following user-list.tsx patterns for layout and bulk selection:

**Page structure:**
```tsx
'use client'

import { useState } from 'react'
import { useFeatureFlagsWithStatus, useToggleFlags } from '@/hooks/use-feature-flag'
import { useAuth } from '@/hooks/use-auth'
import { useAppStore } from '@/lib/stores/app-store'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Switch } from '@/components/ui/switch'
import { Checkbox } from '@/components/ui/checkbox'
import { Button } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
import { TableLoading } from '@/components/ui/loading-states'
import { Flag, School, ToggleLeft, ToggleRight, AlertCircle } from 'lucide-react'
import { toast } from 'sonner'
```

**Layout: "List by flag" (per CONTEXT.md):**
1. Left panel: List of flags (nutricao, estoque_escolar)
2. Right panel: Selected flag's escola toggles

**Components:**

1. **FlagSelector** - Left column card showing all flags
   - Card per flag with name, description
   - Badge showing "X/Y escolas ativas"
   - Click to select flag for editing

2. **EscolaToggleList** - Right column showing escolas for selected flag
   - Header with flag name and description
   - Bulk action bar (when checkboxes selected)
   - List of escolas with:
     - Checkbox for bulk selection
     - Escola name
     - Switch toggle for enabled/disabled
     - Last updated timestamp (if exists)

**Bulk operations (following user-list.tsx pattern):**
- Select multiple escolas via checkboxes
- "Ativar Selecionadas" button
- "Desativar Selecionadas" button
- Clear selection button

**Access control:**
- Check userProfile.tipo_usuario at top
- Show "Acesso negado" if not admin/gestor_sme
- Redirect pattern from existing admin pages

**Empty states:**
- No flags: "Nenhum flag cadastrado" (shouldn't happen with seed data)
- No flag selected: "Selecione um flag para gerenciar"

**Loading states:**
- Use TableLoading component while fetching

**Toast feedback:**
- Success: "Flag {name} atualizado para {N} escolas"
- Error: "Erro ao atualizar flag"

**Responsive layout:**
- Desktop: Two-column grid (flags list | escola toggles)
- Mobile: Stack vertically

Use Portuguese for all UI text.
  </action>
  <verify>
`pnpm typecheck` passes.
Page renders without errors at /dashboard/flags.
Access restricted to admin/gestor_sme users.
Bulk selection follows user-list.tsx pattern.
  </verify>
  <done>
Admin UI displays all flags with per-escola toggles. Bulk operations work. Changes persist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Feature flags admin UI at /dashboard/flags with:
- Flag list with escola count badges
- Per-escola toggle switches
- Bulk select/toggle operations
- Access restricted to admin users
  </what-built>
  <how-to-verify>
1. Start dev server: `cd gestao_fronteira && pnpm dev`
2. Login as admin user
3. Navigate to /dashboard/flags
4. Verify flag list shows "nutricao" and "estoque_escolar"
5. Select a flag and verify all escolas shown with toggles
6. Toggle a single escola - verify switch updates
7. Use bulk selection - select 2+ escolas, click "Ativar Selecionadas"
8. Refresh page - verify changes persisted
9. (Optional) Login as non-admin user - verify access denied
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Page exists at gestao_fronteira/app/(dashboard)/dashboard/flags/page.tsx
2. `pnpm typecheck` passes
3. `pnpm build` passes
4. Page accessible at /dashboard/flags for admin users
5. Flag toggles persist to database
</verification>

<success_criteria>
- Admin can view all flags with escola status
- Admin can toggle individual escola flags
- Admin can bulk toggle multiple escolas
- Non-admin users see access denied
- Changes persist after refresh
- UI follows existing admin patterns
</success_criteria>

<output>
After completion, create `.planning/phases/09-feature-flags/09-03-SUMMARY.md`
</output>
