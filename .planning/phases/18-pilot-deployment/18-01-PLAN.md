---
phase: 18-pilot-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DEPLOYMENT.md
  - docs/ROLLBACK.md
autonomous: true

must_haves:
  truths:
    - "Production deployment accessible at production URL"
    - "Rollback procedure documented and tested conceptually"
    - "Environment variables verified in Vercel dashboard"
  artifacts:
    - path: "docs/DEPLOYMENT.md"
      provides: "Deployment procedure documentation"
      min_lines: 40
    - path: "docs/ROLLBACK.md"
      provides: "Rollback procedure documentation"
      min_lines: 30
  key_links:
    - from: "vercel.json"
      to: "Vercel deployment"
      via: "vercel --prod command"
      pattern: "framework.*nextjs"
---

<objective>
Verify production deployment to Vercel and document deployment/rollback procedures.

Purpose: Ensure EDUCA is accessible in production and team knows how to deploy and rollback if issues arise.
Output: Production deployment verified, DEPLOYMENT.md and ROLLBACK.md documentation.
</objective>

<execution_context>
@C:\Users\Você\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Você\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-pilot-deployment/18-CONTEXT.md
@gestao_fronteira/vercel.json
@gestao_fronteira/.env.production
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and execute production deployment</name>
  <files>gestao_fronteira/</files>
  <action>
    1. Install Vercel CLI if not available: `pnpm add -g vercel`
    2. Login to Vercel: `vercel login` (may require browser auth)
    3. Navigate to gestao_fronteira directory
    4. Run `pnpm build` to verify build succeeds locally
    5. Deploy to production: `vercel --prod`
    6. Verify deployment URL is accessible
    7. Record the production URL for documentation

    Expected: Deployment completes, production URL returns 200 OK on /login
  </action>
  <verify>
    - `vercel --prod` completes without errors
    - Production URL accessible via curl or browser
    - Login page renders correctly
  </verify>
  <done>Production deployment successful, URL confirmed accessible</done>
</task>

<task type="auto">
  <name>Task 2: Create deployment and rollback documentation</name>
  <files>docs/DEPLOYMENT.md, docs/ROLLBACK.md</files>
  <action>
    Create docs/DEPLOYMENT.md with:
    - Prerequisites (Node.js, pnpm, Vercel CLI, Supabase access)
    - Environment variables checklist (from .env.example)
    - Step-by-step deployment commands
    - Post-deployment verification steps
    - Vercel dashboard links for monitoring

    Create docs/ROLLBACK.md with:
    - When to rollback (critical bugs, data corruption, auth issues)
    - Rollback procedure using Vercel CLI: `vercel rollback`
    - Rollback procedure using Vercel dashboard
    - Communication protocol (WhatsApp group notification)
    - Post-rollback verification steps
    - Contact information for escalation

    Use Portuguese for user-facing sections, English for technical commands.
  </action>
  <verify>
    - Both files exist and are readable
    - Commands are copy-pasteable
    - Rollback procedure includes both CLI and dashboard methods
  </verify>
  <done>DEPLOYMENT.md and ROLLBACK.md created with complete procedures</done>
</task>

</tasks>

<verification>
- Production URL returns 200 on /login page
- docs/DEPLOYMENT.md exists with deployment steps
- docs/ROLLBACK.md exists with rollback procedures
- `pnpm build` passes in gestao_fronteira/
</verification>

<success_criteria>
- EDUCA accessible at production Vercel URL
- Team can deploy using documented procedure
- Team can rollback using documented procedure
- Documentation in Portuguese where user-facing
</success_criteria>

<output>
After completion, create `.planning/phases/18-pilot-deployment/18-01-SUMMARY.md`
</output>
