---
phase: 03-login-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/app/(dashboard)/dashboard/page.tsx
autonomous: false

# Goal-backward verification
must_haves:
  truths:
    - "Dashboard stats grid is responsive (4 cols desktop, 2 tablet, 1 mobile)"
    - "Turmas list shows color indicators by série"
    - "Alerts panel displays with semantic colors (warning/error/info)"
    - "Quick actions section is visible and accessible"
  artifacts:
    - path: "gestao_fronteira/app/(dashboard)/dashboard/page.tsx"
      provides: "Responsive dashboard with stats, turmas, alerts, quick actions"
      min_lines: 100
      contains: "grid-cols-4"
  key_links:
    - from: "gestao_fronteira/app/(dashboard)/dashboard/page.tsx"
      to: "components/ui/stat-card"
      via: "import and usage"
      pattern: "import.*StatCard"
    - from: "gestao_fronteira/app/(dashboard)/dashboard/page.tsx"
      to: "components/ui/alert-item"
      via: "import and usage"
      pattern: "import.*AlertItem"
---

<objective>
Refactor Dashboard screen with responsive stats grid, turmas list with color indicators, and alerts panel.

Purpose: Main landing page after login - demonstrates design system in action.
Output: Dashboard with responsive 4→2→1 stats grid, colored turmas list, semantic alerts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context (components):
@.planning/phases/02-layout-composites/02-02-SUMMARY.md

# Current implementation:
@gestao_fronteira/app/(dashboard)/dashboard/page.tsx

# Design reference:
@docs/educa-ui-mockups.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stats grid to be fully responsive</name>
  <files>gestao_fronteira/app/(dashboard)/dashboard/page.tsx</files>
  <action>
Update the stats section grid layout:

1. Grid container: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`
   - Mobile (<640px): 1 column
   - Tablet (640-1023px): 2 columns
   - Desktop (1024px+): 4 columns
2. Use StatCard component from components/ui for each stat:
   - Total Alunos: icon Users, value from data, trend if available
   - Turmas Ativas: icon GraduationCap
   - Frequência Média: icon CalendarCheck, show percentage
   - Professores: icon UserCheck
3. StatCard props: icon, value, label, trend (optional)
4. Card styling already handled by StatCard component

Keep existing data fetching logic, only update layout and component usage.
  </action>
  <verify>pnpm typecheck</verify>
  <done>Stats grid shows 4 cards on desktop, 2 on tablet, 1 on mobile</done>
</task>

<task type="auto">
  <name>Task 2: Update turmas list with color indicators and alerts panel</name>
  <files>gestao_fronteira/app/(dashboard)/dashboard/page.tsx</files>
  <action>
Update turmas section and alerts:

1. Turmas list section:
   - Section title: "Minhas Turmas" with Lexend font
   - Each turma card should have:
     - Left color bar indicator by série (use módulo colors from design tokens)
     - Infantil: bg-pink-500
     - Fundamental I: bg-orange-500
     - Fundamental II: bg-violet-500
   - Display: turma name, série, turno, student count
   - Click action: navigate to turma detail

2. Alerts panel section:
   - Section title: "Alertas Recentes"
   - Use AlertItem component for each alert
   - Severity variants:
     - warning (yellow): baixa frequência, documentação pendente
     - error (red): frequência crítica, prazo vencido
     - info (blue): informativo
     - success (green): meta alcançada
   - Show icon, message, and timestamp

3. Quick actions section:
   - Section title: "Ações Rápidas"
   - Grid of action buttons/cards
   - Actions: "Nova Chamada", "Lançar Notas", "Ver Relatórios", "Cadastrar Aluno"
   - Use Button component with appropriate variant

Layout: 2-column grid on desktop (turmas left, alerts + actions right), stack on mobile.
  </action>
  <verify>pnpm typecheck && pnpm build</verify>
  <done>Turmas show color indicators, alerts use semantic colors, quick actions visible</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Responsive dashboard with stats, colored turmas, semantic alerts</what-built>
  <how-to-verify>
    1. Run: pnpm dev
    2. Visit: http://localhost:3000/dashboard (login first if needed)
    3. Desktop (1024px+):
       - Verify stats grid shows 4 columns
       - Verify turmas and alerts in 2-column layout
    4. Tablet (768-1023px):
       - Verify stats grid shows 2 columns
       - Verify content stacks appropriately
    5. Mobile (<768px):
       - Verify stats grid shows 1 column
       - Verify all sections stack vertically
    6. Check turmas: Each has colored left bar indicator
    7. Check alerts: Yellow for warning, red for error, blue for info
    8. Check quick actions: Buttons visible and clickable
    9. Resize browser: Smooth transitions between breakpoints
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm typecheck passes
- [ ] pnpm build passes
- [ ] Stats grid responsive (4→2→1 columns)
- [ ] Turmas list shows color indicators
- [ ] Alerts display with semantic colors
- [ ] Quick actions section accessible
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Dashboard matches EDUCA design mockup
- Responsive behavior at all breakpoints
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-login-dashboard/03-02-SUMMARY.md`
</output>
