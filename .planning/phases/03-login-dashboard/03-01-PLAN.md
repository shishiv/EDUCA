---
phase: 03-login-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/app/(auth)/login/page.tsx
autonomous: false

# Goal-backward verification
must_haves:
  truths:
    - "Login page shows split layout with gradient hero on left"
    - "EDUCA logo displays with styling"
    - "Form has email, password fields with visible focus states"
    - "Manter conectado checkbox is checked by default"
    - "Esqueci minha senha link is visible and functional"
  artifacts:
    - path: "gestao_fronteira/app/(auth)/login/page.tsx"
      provides: "Split layout login with gradient hero and form"
      min_lines: 80
      contains: "grid-cols-2"
  key_links:
    - from: "gestao_fronteira/app/(auth)/login/page.tsx"
      to: "components/ui/input"
      via: "import and usage"
      pattern: "import.*Input"
---

<objective>
Refactor Login screen to split layout with gradient hero (left) and form (right).

Purpose: First user-facing screen following EDUCA design system - critical for brand impression.
Output: Login page with split layout, gradient hero with features list, and styled form with checkbox.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context (styling patterns):
@.planning/phases/01-design-system-foundation/01-01-SUMMARY.md
@.planning/phases/01-design-system-foundation/01-02-SUMMARY.md

# Current implementation:
@gestao_fronteira/app/(auth)/login/page.tsx

# Design reference:
@docs/educa-ui-mockups.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create split layout structure with gradient hero</name>
  <files>gestao_fronteira/app/(auth)/login/page.tsx</files>
  <action>
Replace current centered single-column layout with split grid layout:

1. Outer container: `min-h-screen grid md:grid-cols-2`
2. Left panel (hero):
   - Full height gradient: `bg-gradient-to-br from-green-600 to-blue-500`
   - Hidden on mobile: `hidden md:flex flex-col justify-center items-center`
   - Content: EDUCA logo/text, tagline "Gestão Educacional Inteligente"
   - Features list with check icons (3-4 key features from mockup)
   - Yellow/gold accent wave or decoration at bottom
3. Right panel (form area):
   - White background, centered content
   - `flex flex-col justify-center items-center p-8`

Keep existing form logic and authentication, only refactor layout/styling.
  </action>
  <verify>pnpm typecheck</verify>
  <done>Login page shows split layout with gradient hero on left, form on right</done>
</task>

<task type="auto">
  <name>Task 2: Style login form with EDUCA design tokens</name>
  <files>gestao_fronteira/app/(auth)/login/page.tsx</files>
  <action>
Update form section styling:

1. Form container: max-w-md width, proper spacing
2. Logo above form: "EDUCA" in Lexend font (font-display), gradient text or styled
3. Email field:
   - Use Input component with EDUCA styling
   - Label "E-mail" associated with input
   - Green focus ring (already in Input component)
4. Password field:
   - Same styling as email
   - Label "Senha"
5. Checkbox row:
   - "Manter conectado" with Checkbox component
   - DEFAULT to checked: `defaultChecked={true}`
6. Submit button:
   - Use Button with variant="default" (green gradient primary)
   - Full width: `w-full`
   - Text: "Entrar"
7. Password reset link:
   - Below button: "Esqueci minha senha"
   - Style: `text-sm text-green-600 hover:underline`
   - Link to `/reset-password` or appropriate route

Use existing UI components: Input, Button, Checkbox, Label from components/ui/.
  </action>
  <verify>pnpm typecheck && pnpm build</verify>
  <done>Form styled with EDUCA design tokens, checkbox defaults to checked, reset link visible</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Split layout login with gradient hero and styled form</what-built>
  <how-to-verify>
    1. Run: pnpm dev
    2. Visit: http://localhost:3000/login
    3. Desktop (>768px): Verify split layout - gradient left, form right
    4. Check gradient: Green to blue gradient on left panel
    5. Check hero content: EDUCA text, tagline, features list
    6. Check form: Email, password with focus states (click to verify green ring)
    7. Check checkbox: "Manter conectado" is checked by default
    8. Check reset link: "Esqueci minha senha" visible below button
    9. Mobile (<768px): Verify hero hides, only form visible
    10. Test login: Ensure existing auth still works
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm typecheck passes
- [ ] pnpm build passes
- [ ] Split layout renders correctly on desktop
- [ ] Form maintains existing authentication functionality
- [ ] Checkbox defaults to checked
- [ ] Password reset link is visible
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Login page matches EDUCA design mockup (split layout)
- Existing auth flow unchanged
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-login-dashboard/03-01-SUMMARY.md`
</output>
