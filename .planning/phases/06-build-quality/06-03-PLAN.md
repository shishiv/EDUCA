---
phase: 06-build-quality
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/components/students/student-form.tsx
  - gestao_fronteira/components/students/index.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "pnpm build completes successfully without module resolution errors"
    - "No imports reference non-existent @/components/ui/enhanced-form"
  artifacts:
    - path: "gestao_fronteira/components/students/index.ts"
      provides: "Student component exports"
      must_not_contain: "student-form"
  key_links:
    - from: "gestao_fronteira/components/students/index.ts"
      to: "gestao_fronteira/components/students/*.tsx"
      via: "export statements"
      pattern: "export.*from"
---

<objective>
Remove dead code that imports non-existent module to fix build failure.

Purpose: The build fails because `student-form.tsx` imports `EnhancedFormProvider` and `EnhancedSubmitActions` from `@/components/ui/enhanced-form` which does not exist. This component is NOT used anywhere in the app (the actual student registration uses `app/(dashboard)/dashboard/alunos/novo/page.tsx` with its own form).

Output: Clean build that passes `pnpm build`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-build-quality/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead student-form.tsx and update exports</name>
  <files>
    gestao_fronteira/components/students/student-form.tsx
    gestao_fronteira/components/students/index.ts
  </files>
  <action>
    1. DELETE the file `gestao_fronteira/components/students/student-form.tsx` entirely
       - This is 856 lines of unused code that imports a non-existent module
       - The actual student registration form is in `app/(dashboard)/dashboard/alunos/novo/page.tsx`

    2. UPDATE `gestao_fronteira/components/students/index.ts`:
       - Remove the export line for EnhancedStudentRegistrationForm/StudentForm
       - Keep all other exports (FaixaEtariaIndicator, StudentProfileHeader, StudentTags, StudentInfoGrid)

    3. Search codebase to confirm no other files import from student-form:
       - `grep -r "EnhancedStudentRegistrationForm\|StudentForm" --include="*.tsx" --include="*.ts" app/`
       - Should return no results (if results found, update those files too)
  </action>
  <verify>
    - `ls gestao_fronteira/components/students/student-form.tsx` returns "No such file"
    - `grep "student-form" gestao_fronteira/components/students/index.ts` returns no matches
    - `grep -r "EnhancedStudentRegistrationForm" gestao_fronteira/app/` returns no matches
  </verify>
  <done>
    - student-form.tsx file deleted
    - index.ts no longer exports EnhancedStudentRegistrationForm or StudentForm
    - No orphaned imports in the codebase
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build passes</name>
  <files>None (verification only)</files>
  <action>
    1. Run `cd gestao_fronteira && pnpm build` to verify the production build completes
    2. The build should now pass since the missing module import is removed
    3. If any other build errors appear, document them for follow-up
  </action>
  <verify>
    - `cd gestao_fronteira && pnpm build` exits with code 0
    - Build output shows "Compiled successfully" or equivalent
  </verify>
  <done>
    - Production build completes without errors
    - BLD-01 (TypeScript type checking in build) now fully functional
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` in gestao_fronteira/ completes successfully
2. No files import from `@/components/ui/enhanced-form`
3. `components/students/index.ts` exports only working components
</verification>

<success_criteria>
- Build passes: `pnpm build` exits 0
- Dead code removed: student-form.tsx deleted
- Exports clean: index.ts has no broken exports
</success_criteria>

<output>
After completion, create `.planning/phases/06-build-quality/06-03-SUMMARY.md`
</output>
