---
phase: 05-aluno-diario-infantil
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/components/students/StudentProfileHeader.tsx
  - gestao_fronteira/components/students/StudentTags.tsx
  - gestao_fronteira/components/students/StudentInfoGrid.tsx
  - gestao_fronteira/components/students/FaixaEtariaIndicator.tsx
  - gestao_fronteira/lib/utils/faixa-etaria.ts
  - gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Perfil shows large avatar (~120px) left, name + info beside"
    - "Two-column grid displays dados pessoais and historico on desktop"
    - "Tags display turma/turno/bolsa familia below name as colored chips"
    - "Faixa etaria indicator appears for Infantil students (calculated from birth date)"
    - "Layout stacks to single column on mobile"
  artifacts:
    - path: "gestao_fronteira/components/students/StudentProfileHeader.tsx"
      provides: "Avatar + name + info + stats header"
      exports: ["StudentProfileHeader"]
    - path: "gestao_fronteira/components/students/StudentTags.tsx"
      provides: "Turma/turno/bolsa familia tag chips"
      exports: ["StudentTags"]
    - path: "gestao_fronteira/components/students/StudentInfoGrid.tsx"
      provides: "Two-column dados pessoais/historico grid"
      exports: ["StudentInfoGrid"]
    - path: "gestao_fronteira/components/students/FaixaEtariaIndicator.tsx"
      provides: "Age group badge for Infantil students"
      exports: ["FaixaEtariaIndicator"]
    - path: "gestao_fronteira/lib/utils/faixa-etaria.ts"
      provides: "Age group calculation helpers"
      exports: ["calculateFaixaEtaria", "FAIXA_ETARIA_CONFIG"]
  key_links:
    - from: "gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/page.tsx"
      to: "StudentProfileHeader, StudentTags, StudentInfoGrid"
      via: "component imports and rendering"
      pattern: "import.*Student"
    - from: "gestao_fronteira/components/students/FaixaEtariaIndicator.tsx"
      to: "gestao_fronteira/lib/utils/faixa-etaria.ts"
      via: "calculateFaixaEtaria function call"
      pattern: "calculateFaixaEtaria\\("
---

<objective>
Refactor the student profile page with modern layout: large avatar header, tag chips, two-column info grid, and faixa etaria indicator for Infantil students.

Purpose: Create a polished student profile that visually communicates key info at a glance and supports the upcoming Diario Infantil module with faixa etaria awareness.
Output: StudentProfileHeader, StudentTags, StudentInfoGrid, FaixaEtariaIndicator components + refactored alunos/[id]/page.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-aluno-diario-infantil/05-CONTEXT.md
@.planning/phases/05-aluno-diario-infantil/05-RESEARCH.md
@gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/page.tsx
@gestao_fronteira/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Faixa Etaria utilities and FaixaEtariaIndicator component</name>
  <files>
    gestao_fronteira/lib/utils/faixa-etaria.ts
    gestao_fronteira/components/students/FaixaEtariaIndicator.tsx
  </files>
  <action>
    1. Create `lib/utils/faixa-etaria.ts`:
       - Define FaixaEtaria type: 'bebes' | 'criancas-bem-pequenas' | 'criancas-pequenas'
       - Create FAIXA_ETARIA_CONFIG with labels, descriptions, month ranges, and colors:
         - bebes: 0-18 months, orange-500
         - criancas-bem-pequenas: 19-47 months, violet-500
         - criancas-pequenas: 48-71 months, sky-500
       - Implement calculateFaixaEtaria(birthDate, referenceDate) using date-fns differenceInMonths
       - Return null if outside early childhood range (0-71 months)

    2. Create `components/students/FaixaEtariaIndicator.tsx`:
       - Accept birthDate prop (string or Date)
       - Calculate faixa using calculateFaixaEtaria
       - Render Badge with appropriate color and label
       - Return null if student is not in Infantil age range
       - Include descriptive tooltip with age range

    Reference RESEARCH.md Pattern 1 for implementation details.
    Use existing Badge component from @/components/ui/badge.
  </action>
  <verify>
    - TypeScript compiles: `cd gestao_fronteira && pnpm typecheck`
    - Utility exports correctly: grep for "calculateFaixaEtaria" in faixa-etaria.ts
  </verify>
  <done>
    - FaixaEtaria type and FAIXA_ETARIA_CONFIG exported
    - calculateFaixaEtaria returns correct faixa for any birth date
    - FaixaEtariaIndicator renders colored badge for Infantil students, null for others
  </done>
</task>

<task type="auto">
  <name>Task 2: Create StudentProfileHeader, StudentTags, StudentInfoGrid components</name>
  <files>
    gestao_fronteira/components/students/StudentProfileHeader.tsx
    gestao_fronteira/components/students/StudentTags.tsx
    gestao_fronteira/components/students/StudentInfoGrid.tsx
  </files>
  <action>
    1. Create `components/students/StudentProfileHeader.tsx`:
       - Props: student (nome, data_nascimento, foto_url?), turma?, stats? (vivencias, frequencia)
       - Layout: Avatar (~120px) left, name + info column beside
       - Avatar uses existing Avatar component with gradient fallback
       - Include FaixaEtariaIndicator for Infantil-age students
       - Stats row below name if provided (frequencia %, vivencias count)

    2. Create `components/students/StudentTags.tsx`:
       - Props: turma?, turno?, bolsaFamilia?, showBolsaFamilia (default false)
       - Render list of colored Badge chips
       - Turma: blue badge, Turno: gray badge
       - Bolsa Familia: yellow badge with warning icon (only if showBolsaFamilia=true per prior decision)
       - Use flex-wrap for responsive chip layout

    3. Create `components/students/StudentInfoGrid.tsx`:
       - Props: student (all personal data fields), responsavel, frequencia, notas
       - Two-column grid on desktop (lg:grid-cols-2), single column on mobile
       - Left column: Dados Pessoais card (nome, nascimento, CPF, sexo, endereco, pais)
       - Right column: Historico card (matriculas list, frequencia summary)
       - Use existing Card components

    Reference CONTEXT.md layout decisions. Follow responsive patterns from prior phases (sm:, lg: breakpoints).
  </action>
  <verify>
    - TypeScript compiles: `cd gestao_fronteira && pnpm typecheck`
    - Components export correctly: grep for "export function Student" in components/students/
  </verify>
  <done>
    - StudentProfileHeader renders avatar + name + stats
    - StudentTags renders turma/turno chips, bolsa familia conditionally
    - StudentInfoGrid renders two-column layout on desktop, stacked on mobile
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor alunos/[id]/page.tsx to use new components</name>
  <files>
    gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/page.tsx
  </files>
  <action>
    1. Import new components:
       - StudentProfileHeader, StudentTags, StudentInfoGrid from @/components/students/
       - FaixaEtariaIndicator (may be used in header)

    2. Refactor page layout:
       - Replace current avatar/card with StudentProfileHeader
       - Add StudentTags below header with turma/turno info
       - Replace grid layout with StudentInfoGrid for dados pessoais + historico
       - Keep existing tabs for academic/frequencia/matriculas/pessoal (or integrate into grid)

    3. Simplify page structure:
       - Remove duplicated layout code now in components
       - Keep loading/error states
       - Maintain existing mock data for now (API integration deferred)

    4. Add navigation link to Diario Infantil (placeholder):
       - Add Button linking to `/dashboard/alunos/[id]/diario` if student is Infantil age
       - Button shows "Ver Diario Infantil" with book icon

    Follow established patterns from Phase 4 for responsive grid (1->2 columns).
  </action>
  <verify>
    - TypeScript compiles: `cd gestao_fronteira && pnpm typecheck`
    - Page renders without errors: manually navigate to /dashboard/alunos/1
    - Profile header shows avatar + name
    - Tags display below name
    - Layout is responsive (check at 1200px and 375px widths)
  </verify>
  <done>
    - Student profile page uses new components
    - Large avatar header with name + info
    - Tags show turma/turno/BF (BF only for gestores)
    - Two-column grid on desktop, single on mobile
    - Link to Diario Infantil appears for Infantil-age students
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd gestao_fronteira && pnpm typecheck` passes
2. Navigate to http://localhost:3000/dashboard/alunos/1
3. Verify profile header layout (avatar ~120px, name beside)
4. Verify tags display below name
5. Verify responsive layout at different breakpoints
6. Verify FaixaEtariaIndicator shows for young students (mock data may need adjustment)
</verification>

<success_criteria>
- [ ] StudentProfileHeader renders avatar + name + stats
- [ ] StudentTags renders colored chip badges
- [ ] StudentInfoGrid renders two-column responsive grid
- [ ] FaixaEtariaIndicator calculates and displays faixa from birth date
- [ ] alunos/[id]/page.tsx uses new components
- [ ] Layout matches CONTEXT.md decisions
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-aluno-diario-infantil/05-01-SUMMARY.md`
</output>
