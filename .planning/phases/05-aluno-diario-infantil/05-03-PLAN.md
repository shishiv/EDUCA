---
phase: 05-aluno-diario-infantil
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - gestao_fronteira/components/diary/DevelopmentReportWriter.tsx
  - gestao_fronteira/components/diary/VivenciasReference.tsx
  - gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/relatorio/page.tsx
autonomous: true

must_haves:
  truths:
    - "Development report shows text areas for each Campo de Experiencia"
    - "Vivencias reference panel displays as sidebar during report writing"
    - "Report generates descriptive text (NEVER grades or numerical indicators)"
    - "Teacher can see student's vivencias while writing report"
    - "Report page accessible from diario page"
  artifacts:
    - path: "gestao_fronteira/components/diary/DevelopmentReportWriter.tsx"
      provides: "Report writing form with 5 campo textareas"
      exports: ["DevelopmentReportWriter"]
    - path: "gestao_fronteira/components/diary/VivenciasReference.tsx"
      provides: "Sidebar showing vivencias for reference"
      exports: ["VivenciasReference"]
    - path: "gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/relatorio/page.tsx"
      provides: "Report writing page route"
      exports: ["default"]
  key_links:
    - from: "gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/relatorio/page.tsx"
      to: "DevelopmentReportWriter, VivenciasReference"
      via: "component imports and side-by-side rendering"
      pattern: "import.*(DevelopmentReportWriter|VivenciasReference)"
    - from: "gestao_fronteira/components/diary/DevelopmentReportWriter.tsx"
      to: "EXPERIENCE_FIELDS_CONFIG"
      via: "config import for field rendering"
      pattern: "EXPERIENCE_FIELDS_CONFIG"
---

<objective>
Create the Development Report writing interface with vivencias reference sidebar, enabling teachers to write descriptive reports per Campo de Experiencia with their observations as context.

Purpose: Provide a focused report writing experience where teachers can reference their recorded vivencias to write comprehensive descriptive development reports.
Output: DevelopmentReportWriter, VivenciasReference components + relatorio page route
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-aluno-diario-infantil/05-CONTEXT.md
@.planning/phases/05-aluno-diario-infantil/05-RESEARCH.md
@gestao_fronteira/components/reports/DescriptiveReportForm.tsx
@gestao_fronteira/types/descriptive-report.ts
@gestao_fronteira/types/diario-infantil.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VivenciasReference sidebar component</name>
  <files>
    gestao_fronteira/components/diary/VivenciasReference.tsx
  </files>
  <action>
    1. Create `components/diary/VivenciasReference.tsx`:
       - Props: vivencias: Vivencia[], selectedCampo?: CampoType, className?
       - Sidebar layout with scroll area
       - Header: "Vivencias Registradas" with count
       - Filter tabs/buttons by Campo de Experiencia (show all or filter by campo)
       - Compact vivencia cards showing:
         - Date (formatted short: "17 Jan")
         - Campo badges (small)
         - Description (truncated, 2 lines)
       - Click to expand full description in place
       - Empty state when no vivencias for selected filter
       - Use ScrollArea from @/components/ui/scroll-area

    2. Design for side-by-side usage:
       - Fixed width (~300-350px) suitable for sidebar
       - Height fills available space
       - Sticky header with filter tabs
       - Scrollable content area

    Reference CONTEXT.md: "professor vê lista das vivências do período para consulta"
  </action>
  <verify>
    - TypeScript compiles: `cd gestao_fronteira && pnpm typecheck`
    - Component exports: grep for "export function VivenciasReference" in components/diary/
  </verify>
  <done>
    - VivenciasReference renders scrollable sidebar
    - Filter by campo works
    - Compact cards show date, campos, description
    - Click expands full description
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DevelopmentReportWriter component</name>
  <files>
    gestao_fronteira/components/diary/DevelopmentReportWriter.tsx
  </files>
  <action>
    1. Create `components/diary/DevelopmentReportWriter.tsx`:
       - Props: studentName, semesterLabel, initialValues?, onSave?, onFinalize?, vivencias?: Vivencia[], isLoading?, disabled?
       - Extend/adapt existing DescriptiveReportForm patterns
       - 5 textareas for each Campo de Experiencia with:
         - Campo header with color and emoji
         - Placeholder text with writing guidance
         - Character count (min 50 chars for finalization)
         - Highlight current campo in VivenciasReference when focused
       - General observations textarea (optional)
       - Progress indicator (X/5 campos filled)
       - Save Draft button (always enabled)
       - Finalize button (enabled when all 5 campos have min content)

    2. CRITICAL: No grades, scores, or numerical indicators:
       - Remove any numeric inputs
       - Placeholders guide descriptive writing
       - Labels use positive, developmental language

    3. Integration point:
       - Accept onCampoFocus callback to sync with VivenciasReference
       - Export current campo state for parent to pass to sidebar

    Reference existing DescriptiveReportForm for patterns, but simplify for this context.
    Use EXPERIENCE_FIELDS_CONFIG from types/descriptive-report.ts.
  </action>
  <verify>
    - TypeScript compiles: `cd gestao_fronteira && pnpm typecheck`
    - Component exports: grep for "export function DevelopmentReportWriter" in components/diary/
    - No grade/score inputs: grep -v for "nota|score|grade" in DevelopmentReportWriter.tsx
  </verify>
  <done>
    - DevelopmentReportWriter renders 5 campo textareas
    - Progress indicator shows completion status
    - Save Draft and Finalize buttons work
    - No numerical grades anywhere
    - onCampoFocus callback triggers on field focus
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Relatorio page with writer and reference sidebar</name>
  <files>
    gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/relatorio/page.tsx
  </files>
  <action>
    1. Create `app/(dashboard)/dashboard/alunos/[id]/diario/relatorio/page.tsx`:
       - Page title "Relatorio de Desenvolvimento" with student name
       - Header with:
         - Back button to diario page
         - Save status indicator
         - Print/Export button (placeholder for future)
       - Two-column layout on desktop:
         - Main content (flex-1): DevelopmentReportWriter
         - Sidebar (w-[350px], hidden on mobile): VivenciasReference
       - Mobile: single column, vivencias accessible via sheet/drawer

    2. State management:
       - Track currently focused campo to sync with sidebar filter
       - Mock vivencias data (reuse from diario page)
       - Mock save/finalize handlers (toast + console.log)

    3. Semester selection (simple):
       - Default to current semester
       - Dropdown to switch 1st/2nd semester
       - Use SEMESTER_CONFIG from types/descriptive-report.ts

    Follow responsive patterns: lg:grid-cols-[1fr,350px] for side-by-side layout.
    Use Sheet for mobile vivencias access.
  </action>
  <verify>
    - TypeScript compiles: `cd gestao_fronteira && pnpm typecheck`
    - Page renders: navigate to /dashboard/alunos/1/diario/relatorio
    - Two-column layout on desktop (check at 1200px width)
    - Single column on mobile (check at 375px width)
  </verify>
  <done>
    - Relatorio page shows writer + vivencias sidebar
    - Campo focus syncs with sidebar filter
    - Save/Finalize buttons work (mock)
    - Responsive layout (sidebar hidden on mobile, accessible via sheet)
    - Navigation from diario page works
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd gestao_fronteira && pnpm typecheck` passes
2. Navigate to http://localhost:3000/dashboard/alunos/1/diario/relatorio
3. Verify two-column layout on desktop
4. Click into a campo textarea and verify sidebar filters
5. Verify no numerical grades anywhere on page
6. Test save draft button (toast appears)
7. Fill all campos and test finalize button
</verification>

<success_criteria>
- [ ] VivenciasReference renders sidebar with vivencias list
- [ ] Sidebar filters by campo when tab clicked
- [ ] DevelopmentReportWriter renders 5 campo textareas
- [ ] Progress indicator shows X/5 completion
- [ ] No grades, scores, or numerical indicators anywhere
- [ ] Relatorio page shows side-by-side layout on desktop
- [ ] Campo focus syncs with sidebar filter
- [ ] Mobile layout works (single column + sheet)
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-aluno-diario-infantil/05-03-SUMMARY.md`
</output>
