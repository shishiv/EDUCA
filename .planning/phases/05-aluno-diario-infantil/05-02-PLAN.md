---
phase: 05-aluno-diario-infantil
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/types/diario-infantil.ts
  - gestao_fronteira/components/diary/CampoExperienciaSelector.tsx
  - gestao_fronteira/components/diary/VivenciaForm.tsx
  - gestao_fronteira/components/diary/VivenciaCard.tsx
  - gestao_fronteira/components/diary/VivenciasTimeline.tsx
  - gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/page.tsx
  - gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/novo/page.tsx
autonomous: true

must_haves:
  truths:
    - "Campo de Experiencia selector shows 5 colored cards in grid"
    - "Multiple campos can be selected (multi-select behavior)"
    - "Vivencia form captures date, campos, and text description"
    - "Child observation cards display campo badges, date, and description"
    - "Timeline groups observations by date"
  artifacts:
    - path: "gestao_fronteira/types/diario-infantil.ts"
      provides: "Types for Vivencia, VivenciaFormData"
      exports: ["Vivencia", "VivenciaFormData", "CampoType"]
    - path: "gestao_fronteira/components/diary/CampoExperienciaSelector.tsx"
      provides: "5 colored cards selector with multi-select"
      exports: ["CampoExperienciaSelector"]
    - path: "gestao_fronteira/components/diary/VivenciaForm.tsx"
      provides: "Vivencia registration form"
      exports: ["VivenciaForm"]
    - path: "gestao_fronteira/components/diary/VivenciaCard.tsx"
      provides: "Single observation card"
      exports: ["VivenciaCard"]
    - path: "gestao_fronteira/components/diary/VivenciasTimeline.tsx"
      provides: "Observations grouped by date"
      exports: ["VivenciasTimeline"]
    - path: "gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/page.tsx"
      provides: "Diario Infantil page route"
      exports: ["default"]
  key_links:
    - from: "gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/page.tsx"
      to: "VivenciasTimeline"
      via: "component import and rendering"
      pattern: "import.*VivenciasTimeline"
    - from: "gestao_fronteira/components/diary/VivenciaForm.tsx"
      to: "CampoExperienciaSelector"
      via: "component usage in form"
      pattern: "CampoExperienciaSelector"
    - from: "gestao_fronteira/components/diary/VivenciaCard.tsx"
      to: "types/diario-infantil.ts"
      via: "Vivencia type import"
      pattern: "import.*Vivencia.*from"
---

<objective>
Create the Diario Infantil module with Campo de Experiencia selector, Vivencia form, observation cards, and timeline view.

Purpose: Enable teachers to register child observations (vivencias) tagged with BNCC Campos de Experiencia for early childhood education documentation.
Output: CampoExperienciaSelector, VivenciaForm, VivenciaCard, VivenciasTimeline components + diario routes
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-aluno-diario-infantil/05-CONTEXT.md
@.planning/phases/05-aluno-diario-infantil/05-RESEARCH.md
@gestao_fronteira/components/ui/campo-experiencia.tsx
@gestao_fronteira/types/descriptive-report.ts
@gestao_fronteira/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create diario-infantil types and CampoExperienciaSelector</name>
  <files>
    gestao_fronteira/types/diario-infantil.ts
    gestao_fronteira/components/diary/CampoExperienciaSelector.tsx
  </files>
  <action>
    1. Create `types/diario-infantil.ts`:
       - Import CampoType from @/components/ui/campo-experiencia
       - Define Vivencia interface:
         - id, aluno_id, turma_id, professor_id: string
         - data_vivencia: string (ISO date)
         - campos_experiencia: CampoType[] (multiple campos allowed)
         - descricao: string
         - observacoes?: string
         - created_at, updated_at: string
       - Define VivenciaFormData interface:
         - data_vivencia: string
         - campos_experiencia: CampoType[] (at least one required)
         - descricao: string
         - observacoes?: string
         - aplicar_multiplos?: boolean (batch mode)
         - alunos_ids?: string[] (for batch mode)
       - Export CAMPOS_EXPERIENCIA config from RESEARCH.md (eu, corpo, tracos, escuta, espacos with colors, emojis)

    2. Create `components/diary/CampoExperienciaSelector.tsx`:
       - Props: selectedCampos: CampoType[], onSelectionChange: (campos: CampoType[]) => void, disabled?: boolean
       - Render 5 cards in responsive grid (2-3 columns on mobile, 5 on desktop)
       - Use existing CampoExperiencia component as base but add selection state
       - Selected state: ring/border highlight + checkmark overlay
       - Toggle behavior: click adds/removes from selection
       - Use existing CSS variables (--campo-eu, etc.) for colors
       - Keyboard accessible (Enter/Space to toggle)
  </action>
  <verify>
    - TypeScript compiles: `cd gestao_fronteira && pnpm typecheck`
    - Types export correctly: grep for "export interface Vivencia" in types/diario-infantil.ts
  </verify>
  <done>
    - Vivencia and VivenciaFormData types defined
    - CAMPOS_EXPERIENCIA config exported
    - CampoExperienciaSelector renders 5 cards with multi-select behavior
    - Selection state visually indicated with ring/checkmark
  </done>
</task>

<task type="auto">
  <name>Task 2: Create VivenciaForm and VivenciaCard components</name>
  <files>
    gestao_fronteira/components/diary/VivenciaForm.tsx
    gestao_fronteira/components/diary/VivenciaCard.tsx
  </files>
  <action>
    1. Create `components/diary/VivenciaForm.tsx`:
       - Props: onSubmit: (data: VivenciaFormData) => Promise<void>, studentName: string, initialData?, isLoading?, disabled?
       - Use react-hook-form + zod for validation
       - Fields:
         - data_vivencia: date input, defaults to today
         - campos_experiencia: CampoExperienciaSelector (required, at least 1)
         - descricao: Textarea (required, min 20 chars)
         - observacoes: Textarea (optional)
       - Submit button with loading state
       - Cancel button
       - Form validation: at least one campo selected, description not empty

    2. Create `components/diary/VivenciaCard.tsx`:
       - Props: vivencia: Vivencia, onEdit?: () => void, onDelete?: () => void
       - Card layout:
         - Header: date (formatted), edit/delete actions (if callbacks provided)
         - Campos: row of colored Badge chips (one per campo)
         - Description: truncated text (3 lines max)
         - Optional observations in smaller text
       - Use existing Badge component with campo colors
       - Hover state with subtle shadow

    Reference CONTEXT.md decisions for form UX (modal/full page, escopo individual).
    Use date-fns for date formatting.
  </action>
  <verify>
    - TypeScript compiles: `cd gestao_fronteira && pnpm typecheck`
    - Components export correctly: grep for "export function Vivencia" in components/diary/
  </verify>
  <done>
    - VivenciaForm renders date, campos selector, description fields
    - Form validates at least one campo selected
    - VivenciaCard displays vivencia with campo badges and description
    - Cards have edit/delete action buttons when callbacks provided
  </done>
</task>

<task type="auto">
  <name>Task 3: Create VivenciasTimeline and Diario pages</name>
  <files>
    gestao_fronteira/components/diary/VivenciasTimeline.tsx
    gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/page.tsx
    gestao_fronteira/app/(dashboard)/dashboard/alunos/[id]/diario/novo/page.tsx
  </files>
  <action>
    1. Create `components/diary/VivenciasTimeline.tsx`:
       - Props: vivencias: Vivencia[], groupBy?: 'day' | 'week', onEditVivencia?, onDeleteVivencia?
       - Group vivencias by date using date-fns
       - Render date headers (e.g., "Segunda, 17 de Janeiro" or "Semana 3 - Janeiro")
       - Render VivenciaCards under each date header
       - Empty state message when no vivencias

    2. Create `app/(dashboard)/dashboard/alunos/[id]/diario/page.tsx`:
       - Page title "Diario Infantil" with student name
       - Header with:
         - Back button to student profile
         - "Nova Vivencia" button linking to ./novo
         - Link to relatario page
       - VivenciasTimeline showing student's observations
       - Mock data for now (2-3 sample vivencias)
       - Loading skeleton during data fetch

    3. Create `app/(dashboard)/dashboard/alunos/[id]/diario/novo/page.tsx`:
       - Page title "Registrar Vivencia"
       - VivenciaForm component
       - On submit: show toast, redirect back to diario page
       - Mock implementation (console.log data, simulate save)

    Use existing page patterns from chamada for consistent layout.
    Follow responsive patterns from prior phases.
  </action>
  <verify>
    - TypeScript compiles: `cd gestao_fronteira && pnpm typecheck`
    - Pages render: navigate to /dashboard/alunos/1/diario
    - New vivencia form shows at /dashboard/alunos/1/diario/novo
  </verify>
  <done>
    - VivenciasTimeline groups and displays observations by date
    - Diario page shows student's vivencias with timeline view
    - Nova vivencia page renders VivenciaForm
    - Navigation between pages works
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd gestao_fronteira && pnpm typecheck` passes
2. Navigate to http://localhost:3000/dashboard/alunos/1/diario
3. Verify timeline displays grouped observations
4. Click "Nova Vivencia" and verify form renders
5. Select multiple campos and verify multi-select works
6. Submit form and verify toast + redirect
</verification>

<success_criteria>
- [ ] Vivencia types defined in types/diario-infantil.ts
- [ ] CampoExperienciaSelector renders 5 cards with multi-select
- [ ] VivenciaForm validates and captures vivencia data
- [ ] VivenciaCard displays observation with campo badges
- [ ] VivenciasTimeline groups observations by date
- [ ] Diario page route works at /dashboard/alunos/[id]/diario
- [ ] Nova vivencia page at /dashboard/alunos/[id]/diario/novo
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-aluno-diario-infantil/05-02-SUMMARY.md`
</output>
