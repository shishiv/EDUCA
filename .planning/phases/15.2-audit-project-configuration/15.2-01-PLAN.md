---
phase: 15.2-audit-project-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/eslint.config.mjs
  - gestao_fronteira/package.json
autonomous: true

must_haves:
  truths:
    - "pnpm lint runs without circular reference error"
    - "ESLint reports actual code issues, not configuration errors"
    - "Lint rules match project conventions (no-unused-vars, no-any warn, no-console selective)"
  artifacts:
    - path: "gestao_fronteira/eslint.config.mjs"
      provides: "Working ESLint 9 flat config without FlatCompat"
      contains: "defineConfig"
  key_links:
    - from: "eslint.config.mjs"
      to: "@typescript-eslint/eslint-plugin"
      via: "plugin configuration"
      pattern: "typescript-eslint"
---

<objective>
Fix broken ESLint configuration that causes circular reference errors.

Purpose: ESLint is currently broken (`TypeError: Converting circular structure to JSON`), blocking CI/CD and code quality enforcement. This is a critical blocker for the project.

Output: Working ESLint 9 flat config using native format instead of FlatCompat bridge.
</objective>

<execution_context>
@C:\Users\shiv\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\shiv\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-audit-project-configuration/15.2-RESEARCH.md
@gestao_fronteira/eslint.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace FlatCompat with native ESLint 9 flat config</name>
  <files>gestao_fronteira/eslint.config.mjs</files>
  <action>
  Rewrite eslint.config.mjs to use native ESLint 9 flat config format:

  1. Remove FlatCompat imports and usage (causes circular reference)
  2. Import plugins directly:
     - `@typescript-eslint/eslint-plugin` as tsPlugin
     - `@typescript-eslint/parser` as tsParser
     - `@next/eslint-plugin-next` as nextPlugin
  3. Use `globalIgnores` for ignored paths (replaces `ignores` object)
  4. Configure plugins in native format with languageOptions

  Keep existing rules:
  - '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }]
  - '@typescript-eslint/explicit-function-return-type': 'off'
  - '@typescript-eslint/no-explicit-any': 'warn'
  - 'no-console': ['warn', { allow: ['warn', 'error', 'info'] }]
  - 'no-eval': 'error'
  - 'no-implied-eval': 'error'
  - '@next/next/no-img-element': 'error'
  - 'react/no-unknown-property': 'off'
  - 'react/prop-types': 'off'

  Reference pattern from RESEARCH.md:
  ```javascript
  import { defineConfig, globalIgnores } from 'eslint/config'
  import tsPlugin from '@typescript-eslint/eslint-plugin'
  import tsParser from '@typescript-eslint/parser'
  import nextPlugin from '@next/eslint-plugin-next'
  ```
  </action>
  <verify>Run `cd gestao_fronteira && pnpm.cmd lint` - should NOT produce circular reference error</verify>
  <done>ESLint runs and reports code issues (warnings/errors are OK, configuration errors are NOT)</done>
</task>

<task type="auto">
  <name>Task 2: Install missing ESLint dependencies if needed</name>
  <files>gestao_fronteira/package.json</files>
  <action>
  Check if required plugins are installed, install if missing:

  1. Verify `@typescript-eslint/eslint-plugin` is in devDependencies
  2. Verify `@typescript-eslint/parser` is in devDependencies
  3. Verify `@next/eslint-plugin-next` is in devDependencies (may be bundled with eslint-config-next)

  If any are missing, run:
  ```bash
  cd gestao_fronteira && pnpm.cmd add -D @typescript-eslint/eslint-plugin @typescript-eslint/parser
  ```

  Note: eslint-config-next can be removed if not needed by native config.
  </action>
  <verify>Run `cd gestao_fronteira && pnpm.cmd lint` - should complete successfully</verify>
  <done>All required ESLint plugins are installed and lint command works</done>
</task>

<task type="auto">
  <name>Task 3: Verify lint catches actual issues</name>
  <files>None (verification only)</files>
  <action>
  Run lint to verify it catches real code issues:

  1. Run `pnpm lint` and capture output
  2. Verify output includes:
     - File paths being linted
     - Any warnings/errors about code (not config)
  3. If lint reports 0 issues, that's fine (means code is clean)
  4. If lint crashes with config error, Task 1 needs revision

  Expected output format:
  ```
  /path/to/file.tsx
    1:1  warning  ...
  ```

  NOT expected:
  ```
  TypeError: Converting circular structure to JSON
  ```
  </action>
  <verify>Lint output shows file paths or "0 problems", not configuration errors</verify>
  <done>ESLint configuration is validated as working correctly</done>
</task>

</tasks>

<verification>
1. `pnpm lint` in gestao_fronteira/ completes without crashing
2. Output shows file paths being linted (or "0 problems found")
3. No "circular structure" or "Converting" errors
4. No "Cannot find module" errors for ESLint plugins
</verification>

<success_criteria>
- ESLint runs successfully without configuration errors
- Lint rules are enforced (no-unused-vars, no-any, etc.)
- CI/CD can now use lint step without failure
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-audit-project-configuration/15.2-01-SUMMARY.md`
</output>
