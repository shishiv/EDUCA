---
phase: 15.2-audit-project-configuration
plan: 03
type: execute
wave: 2
depends_on: ["15.2-01", "15.2-02"]
files_modified:
  - gestao_fronteira/tsconfig.json
  - gestao_fronteira/next.config.js
autonomous: true

must_haves:
  truths:
    - "TypeScript target is modern (es2023+), not legacy (es5)"
    - "next.config uses stable patterns, not deprecated experimental"
    - "Build and typecheck pass with updated configurations"
  artifacts:
    - path: "gestao_fronteira/tsconfig.json"
      provides: "Modern TypeScript configuration"
      contains: "es2023"
    - path: "gestao_fronteira/next.config.js"
      provides: "Next.js 16 compatible configuration"
      min_lines: 50
  key_links:
    - from: "tsconfig.json"
      to: "pnpm typecheck"
      via: "TypeScript compiler"
      pattern: "compilerOptions"
---

<objective>
Modernize TypeScript and Next.js configurations for 2026 best practices.

Purpose: Update outdated settings (es5 target from 2015, experimental flags that are now stable) to modern standards for better performance and future compatibility.

Output: Modern tsconfig.json and next.config.js following current best practices.
</objective>

<execution_context>
@C:\Users\shiv\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\shiv\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-audit-project-configuration/15.2-RESEARCH.md
@gestao_fronteira/tsconfig.json
@gestao_fronteira/next.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tsconfig.json to modern settings</name>
  <files>gestao_fronteira/tsconfig.json</files>
  <action>
  Update tsconfig.json compilerOptions:

  1. Change `target` from "es5" to "es2023"
     - ES5 is 10+ years outdated
     - All modern browsers support ES2023
     - Smaller output, better performance

  2. Keep existing settings that are correct:
     - `lib`: ["dom", "dom.iterable", "esnext"]
     - `module`: "esnext"
     - `moduleResolution`: "bundler"
     - `jsx`: "react-jsx"
     - `strict`: true
     - `noEmit`: true
     - `incremental`: true
     - `paths`: { "@/*": ["./*"] }

  3. Consider adding (optional, low risk):
     - `verbatimModuleSyntax`: true - TypeScript 5.9 recommended

  DO NOT add high-risk options yet:
     - `noUncheckedIndexedAccess` - May cause many new errors

  Resulting tsconfig should be minimal changes from current.
  </action>
  <verify>Run `cd gestao_fronteira && pnpm.cmd typecheck` - must pass</verify>
  <done>TypeScript compiles successfully with modern target</done>
</task>

<task type="auto">
  <name>Task 2: Update next.config.js for Next.js 16 patterns</name>
  <files>gestao_fronteira/next.config.js</files>
  <action>
  Update next.config.js to use stable patterns:

  1. Server Actions config:
     - In Next.js 15+, serverActions is stable
     - HOWEVER: Keep under experimental if it works (low priority change)
     - Only change if causing issues

  2. optimizePackageImports:
     - This IS correctly under experimental (still experimental flag)
     - DO NOT move this

  3. Keep bundle analyzer wrapper:
     ```javascript
     const withBundleAnalyzer = require('@next/bundle-analyzer')({
       enabled: process.env.ANALYZE === 'true',
     })
     ```

  4. Keep all existing settings:
     - images configuration (remotePatterns, formats, sizes)
     - reactStrictMode: true
     - compress: true
     - headers() function

  5. OPTIONAL (low priority):
     - Convert from CommonJS to ESM (module.exports -> export default)
     - Only if not breaking bundle-analyzer wrapper

  IMPORTANT: This is a low-risk modernization. If build breaks, revert and skip.
  </action>
  <verify>Run `cd gestao_fronteira && pnpm.cmd build` - must pass</verify>
  <done>Next.js config updated with stable patterns where applicable</done>
</task>

<task type="auto">
  <name>Task 3: Create CONFIG-AUDIT.md documenting changes and future work</name>
  <files>.planning/codebase/CONFIG-AUDIT.md</files>
  <action>
  Create documentation of configuration audit findings:

  1. Document what was changed:
     - ESLint: FlatCompat removed, native flat config
     - Dependencies: Unused deps removed, @types moved
     - tsconfig: target updated to es2023
     - next.config: Any changes made

  2. Document what was deferred (Wave 3 from research):
     - Tailwind CSS v4 migration (major breaking changes)
     - Zod v4 migration (requires codemod)
     - Zustand v5 migration (API changes)

  3. Document remaining outdated dependencies:
     - List packages with major updates available
     - Note breaking change risk level

  4. Include commands for future maintenance:
     - `pnpm outdated` - Check for updates
     - `npx knip --dependencies` - Check unused deps
     - `pnpm update --interactive` - Safe updates

  Format as markdown with tables for easy reference.
  </action>
  <verify>File exists at .planning/codebase/CONFIG-AUDIT.md</verify>
  <done>Configuration audit documented for future reference</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes with updated tsconfig
2. `pnpm build` passes with updated next.config
3. `pnpm dev --turbo` starts without errors
4. CONFIG-AUDIT.md created with findings and recommendations
</verification>

<success_criteria>
- TypeScript target modernized to es2023
- Next.js config follows current best practices
- All changes verified with build/typecheck
- Future work documented for reference
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-audit-project-configuration/15.2-03-SUMMARY.md`
</output>
