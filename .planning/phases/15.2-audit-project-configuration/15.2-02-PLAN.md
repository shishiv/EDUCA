---
phase: 15.2-audit-project-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/package.json
  - gestao_fronteira/pnpm-lock.yaml
  - gestao_fronteira/knip.json
autonomous: true

must_haves:
  truths:
    - "Unused dependencies are removed from package.json"
    - "@types packages are in devDependencies, not dependencies"
    - "Build still passes after dependency changes"
  artifacts:
    - path: "gestao_fronteira/package.json"
      provides: "Clean dependency list with proper categorization"
      min_lines: 80
  key_links:
    - from: "package.json"
      to: "pnpm-lock.yaml"
      via: "pnpm install"
      pattern: "lockfile updated"
---

<objective>
Clean up package.json by removing unused dependencies and moving @types to devDependencies.

Purpose: Reduce bundle size, improve maintainability, and follow npm best practices for dependency categorization.

Output: Leaner package.json with verified unused dependencies removed.
</objective>

<execution_context>
@C:\Users\shiv\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\shiv\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-audit-project-configuration/15.2-RESEARCH.md
@gestao_fronteira/package.json
@gestao_fronteira/knip.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify unused dependencies before removal</name>
  <files>None (verification only)</files>
  <action>
  Verify each "unused" dependency identified by knip before removal:

  1. Run knip to get current unused deps list:
     `cd gestao_fronteira && npx knip --dependencies`

  2. For each reported unused dependency, search codebase:
     - `@radix-ui/react-collapsible` - Search for "Collapsible" in components
     - `@radix-ui/react-radio-group` - Search for "RadioGroup" in components
     - `@radix-ui/react-slider` - Search for "Slider" in components
     - `@radix-ui/react-toggle` - Search for "Toggle" in components
     - `@radix-ui/react-toggle-group` - Search for "ToggleGroup" in components
     - `@tanstack/react-query-devtools` - Only used in dev, may be dynamically imported
     - `input-otp` - Search for "InputOTP" or "OTP" in components
     - `react-resizable-panels` - Search for "ResizablePanel" in components
     - `recharts` - Search for "recharts" or chart components (MAY BE USED)
     - `vaul` - Search for "Drawer" from vaul in components
     - `zustand` - Search for "create" from zustand or store files

  3. For devDependencies:
     - `@testing-library/dom` - Check if tests use it
     - `@testing-library/react` - Check if tests use it
     - `eslint-config-next` - Needed for FlatCompat (check after Plan 01)
     - `supabase` - CLI tool, may be needed for db:types script

  IMPORTANT: recharts and zustand are commonly dynamically imported. Verify with grep before removing.
  </action>
  <verify>Create list of CONFIRMED unused dependencies (verified by codebase search)</verify>
  <done>List of safe-to-remove dependencies documented</done>
</task>

<task type="auto">
  <name>Task 2: Remove confirmed unused dependencies</name>
  <files>gestao_fronteira/package.json, gestao_fronteira/pnpm-lock.yaml</files>
  <action>
  Remove only CONFIRMED unused dependencies from Task 1:

  1. For each confirmed unused dep:
     ```bash
     cd gestao_fronteira && pnpm.cmd remove <package-name>
     ```

  2. Expected safe removals (based on research, verify first):
     - `@radix-ui/react-collapsible` - Not used in any component
     - `@radix-ui/react-radio-group` - Not used in any component
     - `@radix-ui/react-slider` - Not used in any component
     - `@radix-ui/react-toggle` - Not used in any component
     - `@radix-ui/react-toggle-group` - Not used in any component
     - `input-otp` - Not used in any component
     - `vaul` - Not used (Drawer from shadcn uses different implementation)

  3. DO NOT REMOVE without verification:
     - `recharts` - May be used in dashboard charts
     - `zustand` - May be used in state stores
     - `@tanstack/react-query-devtools` - Dev tool, OK to keep
     - `react-resizable-panels` - May be used in layout

  4. Run `pnpm install` to update lockfile
  </action>
  <verify>Run `cd gestao_fronteira && pnpm.cmd build` - build must pass</verify>
  <done>Unused dependencies removed, build passes</done>
</task>

<task type="auto">
  <name>Task 3: Move @types packages to devDependencies</name>
  <files>gestao_fronteira/package.json</files>
  <action>
  Move type packages from dependencies to devDependencies:

  1. Identify @types packages in dependencies:
     - `@types/node`
     - `@types/react`
     - `@types/react-dom`

  2. Move them using pnpm:
     ```bash
     cd gestao_fronteira
     pnpm.cmd remove @types/node @types/react @types/react-dom
     pnpm.cmd add -D @types/node @types/react @types/react-dom
     ```

  3. Also move other build-time only packages:
     - `autoprefixer` - PostCSS plugin, only needed at build
     - `postcss` - Build tool
     - `typescript` - Compiler, not runtime

  IMPORTANT: Test build after moving to ensure types are available at build time.
  </action>
  <verify>Run `cd gestao_fronteira && pnpm.cmd build && pnpm.cmd typecheck` - both must pass</verify>
  <done>Type packages in devDependencies, build passes</done>
</task>

</tasks>

<verification>
1. `pnpm build` passes in gestao_fronteira/
2. `pnpm typecheck` passes
3. `npx knip --dependencies` shows fewer unused dependencies
4. package.json has @types in devDependencies
</verification>

<success_criteria>
- Unused dependencies removed (verified by search before removal)
- @types packages properly categorized as devDependencies
- Build and typecheck both pass after changes
- Bundle size potentially reduced (fewer runtime deps)
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-audit-project-configuration/15.2-02-SUMMARY.md`
</output>
