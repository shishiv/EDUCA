---
phase: 15.1-dead-code-audit-lsp
plan: 03
type: execute
wave: 2
depends_on: ["15.1-01", "15.1-02"]
files_modified:
  - gestao_fronteira/types/*.ts
  - .planning/codebase/DEAD-CODE-AUDIT.md
autonomous: true

must_haves:
  truths:
    - "All type exports in types/ verified for usage via LSP findReferences"
    - "Audit findings documented in DEAD-CODE-AUDIT.md"
    - "Final verification passes (typecheck, lint, build)"
  artifacts:
    - path: ".planning/codebase/DEAD-CODE-AUDIT.md"
      provides: "Audit methodology and findings documentation"
      min_lines: 100
    - path: "gestao_fronteira/types/*.ts"
      provides: "Cleaned type definition files"
  key_links:
    - from: ".planning/codebase/DEAD-CODE-AUDIT.md"
      to: "git log"
      via: "commit references"
      pattern: "refactor:.*remove"
---

<objective>
Audit types/ directory (9 files) and create comprehensive audit report documenting methodology and findings.

Purpose: Complete the dead code audit and document the process for future maintenance and audit trail.
Output: Clean types/ directory, DEAD-CODE-AUDIT.md report with all findings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15.1-dead-code-audit-lsp/15.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit types/ directory</name>
  <files>gestao_fronteira/types/*.ts</files>
  <action>
Audit all 9 type definition files:
- types/bolsa-familia.ts
- types/database.ts
- types/descriptive-report.ts
- types/diario-classe.ts
- types/diario-infantil.ts
- types/feature-flags.ts
- types/grades.ts
- types/lesson-content.ts
- types/supabase.ts

For each file:
1. `mcp__lsp__documentSymbol` to get all exported types/interfaces
2. `mcp__lsp__findReferences` for each export
3. Check if type is used:
   - In runtime code (components, lib, pages)
   - In other type files (cross-references)
   - In API response typing

Special considerations for types/:
- Type-only imports (`import type { X }`) may not create runtime references
- Check if types are used for type guards or assertions
- Some types may be exported for external consumers (API contract documentation)

For unused types:
1. Verify not needed for type inference (may be used implicitly)
2. Run `pnpm typecheck` before removal to establish baseline
3. Remove the type export
4. Run `pnpm typecheck` after - if it fails, type was needed
5. Commit atomically if removal succeeds

Note: types/supabase.ts and types/database.ts are likely auto-generated or used for Supabase typing - audit with extra care.
  </action>
  <verify>
    - All 9 type files audited via LSP
    - Unused type exports removed
    - `pnpm typecheck` passes after removals
    - Type changes don't break runtime behavior
  </verify>
  <done>types/ directory audit complete, unused type exports removed</done>
</task>

<task type="auto">
  <name>Task 2: Create DEAD-CODE-AUDIT.md report</name>
  <files>.planning/codebase/DEAD-CODE-AUDIT.md</files>
  <action>
**Step 1: Extract findings from git log (NOT from summary files)**
```bash
# Get all removal commits from this phase
git log --oneline --all-match --grep="refactor" --grep="remove" --since="2026-01-23" > phase-commits.txt

# Get detailed commit info with files changed
git log --name-only --all-match --grep="refactor" --grep="remove" --since="2026-01-23" > phase-commits-detailed.txt

# Count metrics
COMMITS_COUNT=$(wc -l < phase-commits.txt)
FILES_REMOVED=$(git log --name-status --all-match --grep="refactor" --grep="remove" --since="2026-01-23" | grep "^D" | wc -l)
```

**Step 2: Create comprehensive audit report at `.planning/codebase/DEAD-CODE-AUDIT.md`**

```markdown
# Dead Code Audit Report

**Date:** [execution date - use current date]
**Phase:** 15.1 - Dead Code Audit via LSP
**Auditor:** Claude (automated)

## Executive Summary

- **Total files audited:** ~234 (84 lib/ + 141 components/ + 9 types/)
- **Dead code items found:** [count from git log]
- **Files removed:** [from FILES_REMOVED]
- **Exports removed:** [count from commits]
- **Commits:** [COMMITS_COUNT]

## Methodology

### Tools Used
1. **LSP findReferences** - Primary audit method via `mcp__lsp__findReferences`
2. **knip** - Validation and batch scanning
3. **TypeScript compiler** - Verification via `pnpm typecheck`

### Audit Process
1. Run knip scan to identify candidates
2. For each candidate, verify with LSP findReferences
3. Cross-check for dynamic imports and barrel re-exports
4. Remove confirmed dead code with atomic commits
5. Verify with typecheck/lint/build after each batch

### Directories Audited
- `lib/` - 84 files (API services, hooks, utilities)
- `components/` - 141 files (UI components, excluding shadcn/ui)
- `types/` - 9 files (TypeScript type definitions)

### Directories Excluded
- `app/` - Next.js framework entry points (but exports still audited via knip)

## Findings

[Populate from git log --name-status output, organized by directory]

### lib/ Directory

| File | Export | Status | Commit |
|------|--------|--------|--------|
| [extract from phase-commits-detailed.txt - lib/ entries] |

### components/ Directory

| File | Export | Status | Commit |
|------|--------|--------|--------|
| [extract from phase-commits-detailed.txt - components/ entries] |

### types/ Directory

| File | Export | Status | Commit |
|------|--------|--------|--------|
| [extract from Task 1 results] |

## False Positives Encountered

Document any items flagged by knip or LSP that were NOT removed and why:
- [list items with reasons from execution notes]

## Recommendations

1. **Run knip periodically** - Add to CI or pre-commit hook
2. **Review new exports** - Ensure new code is actually used
3. **Barrel file hygiene** - Keep index.ts files minimal

## Verification

Final verification results:
- `pnpm typecheck`: [PASS/FAIL]
- `pnpm lint`: [PASS/FAIL]
- `pnpm build`: [PASS/FAIL]
- `knip --include exports`: [remaining count]

## Appendix

### Knip Configuration
[cat gestao_fronteira/knip.json]

### Commit Log
[paste phase-commits.txt content]
```

**Step 3: Verify report completeness**
- Report has all three directory sections populated
- Commit log appendix has actual commits
- Metrics are calculated from real data
  </action>
  <verify>
    - DEAD-CODE-AUDIT.md exists at .planning/codebase/
    - Report contains methodology section
    - Report contains findings tables for all three directories
    - Report includes verification results
  </verify>
  <done>Comprehensive audit report created documenting all findings and methodology</done>
</task>

<task type="auto">
  <name>Task 3: Final verification and commit</name>
  <files>gestao_fronteira/package.json</files>
  <action>
Run final verification suite:

1. TypeScript check:
   ```bash
   cd gestao_fronteira && pnpm typecheck
   ```

2. ESLint check:
   ```bash
   pnpm lint
   ```

3. Production build:
   ```bash
   pnpm build
   ```

4. Run knip to confirm improvements:
   ```bash
   npx knip --include exports,files
   ```
   Compare with initial results to quantify dead code removed.

5. Commit the audit report:
   ```bash
   git add .planning/codebase/DEAD-CODE-AUDIT.md gestao_fronteira/knip.json
   git commit -m "docs(15.1): dead code audit report and knip configuration

- Audit methodology documented
- [X] items removed from lib/
- [Y] items removed from components/
- [Z] items removed from types/
- knip.json configured for future audits"
   ```

6. Update STATE.md to reflect phase completion.

If any verification fails:
- Identify which removal caused the issue
- Revert that specific commit
- Update the audit report to document the false positive
- Re-run verification
  </action>
  <verify>
    - `pnpm typecheck` passes with 0 errors
    - `pnpm lint` passes with 0 errors
    - `pnpm build` succeeds
    - Audit report committed
    - STATE.md updated
  </verify>
  <done>Phase 15.1 complete - dead code audit finished with full documentation</done>
</task>

</tasks>

<verification>
Final phase verification:
1. All three directories audited (lib/, components/, types/)
2. Dead code removed with atomic, documented commits
3. DEAD-CODE-AUDIT.md exists with complete findings
4. Final verification suite passes (typecheck, lint, build)
5. knip configured for future maintenance
</verification>

<success_criteria>
- DCA-01 satisfied: All exports audited via LSP findReferences
- DCA-02 satisfied: Dead code removed with git history preservation
- DCA-03 satisfied: Audit methodology and findings documented
- Zero TypeScript errors
- Zero lint errors
- Production build succeeds
- Audit report committed to .planning/codebase/
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-dead-code-audit-lsp/15.1-03-SUMMARY.md`
</output>
