---
phase: 15.1-dead-code-audit-lsp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gestao_fronteira/components/**/*.tsx
autonomous: true

must_haves:
  truths:
    - "All exported components verified for usage via LSP findReferences"
    - "Confirmed dead components removed with atomic commits"
    - "typecheck and lint pass after removals"
  artifacts:
    - path: "gestao_fronteira/components/**/*.tsx"
      provides: "Cleaned component files"
  key_links:
    - from: "gestao_fronteira/components/**/*.tsx"
      to: "typecheck"
      via: "pnpm typecheck"
      pattern: "tsc.*noEmit"
---

<objective>
Audit components/ directory (141 files) for unused component exports using LSP findReferences.

Purpose: components/ may contain legacy or orphaned components from UI refactors - removing them reduces bundle size and maintenance burden.
Output: Clean components/ directory with all dead components removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15.1-dead-code-audit-lsp/15.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run knip scan focused on components/</name>
  <files>gestao_fronteira/components/**/*.tsx</files>
  <action>
1. Run knip specifically checking for unused component files and exports:
   ```bash
   cd gestao_fronteira
   npx knip --include exports,files 2>&1 | grep -E "^components/" | tee knip-components-results.txt
   ```

2. Categorize results by subdirectory:
   - components/admin/ - Admin-specific components
   - components/attendance/ - Attendance/chamada components
   - components/auth/ - Authentication components
   - components/branding/ - Branding components (may be entirely unused)
   - components/calendario/ - Calendar components
   - components/classes/ - Class management components
   - components/dashboard/ - Dashboard components
   - components/diary/ - Diario de classe components
   - components/grades/ - Grade input components
   - components/help/ - Help system components
   - components/layout/ - Layout components
   - components/providers/ - Context providers
   - components/reports/ - Report components
   - components/students/ - Student components
   - components/tutorial/ - Tutorial overlay
   - components/turmas/ - Turma card components
   - components/ui/ - SKIP (shadcn/ui - generated code)

3. Create prioritized list based on knip output.

Note: components/ui/ is excluded from audit as these are shadcn/ui generated components that intentionally export many unused variants.
  </action>
  <verify>
    - knip-components-results.txt exists with component-specific results
    - Results categorized by subdirectory
    - Prioritized list of candidates created
  </verify>
  <done>Knip scan of components/ completed with categorized results</done>
</task>

<task type="auto">
  <name>Task 2: LSP audit of high-priority component subdirectories</name>
  <files>gestao_fronteira/components/admin/*.tsx, gestao_fronteira/components/branding/*.tsx, gestao_fronteira/components/calendario/*.tsx, gestao_fronteira/components/help/*.tsx, gestao_fronteira/components/tutorial/*.tsx</files>
  <action>
Audit component subdirectories that are likely to have dead code (based on PAGE-AUDIT.md findings and refactoring history):

**Priority 1 - Likely unused:**
- components/branding/ - platform-name-preview.tsx (associated page was deleted in Phase 14)
- components/tutorial/ - TutorialOverlay.tsx (may be unused)
- components/help/ - HelpSystem.tsx (check if integrated)

**Priority 2 - Admin components:**
- components/admin/users/ - bulk-user-operations.tsx, user-form.tsx, user-list.tsx
- Check each component's default export with findReferences

**Priority 3 - Calendar/events:**
- components/calendario/ - evento-form.tsx, evento-list.tsx
- Check if these are used in any page

For each file:
1. `mcp__lsp__documentSymbol` to get exported symbols
2. `mcp__lsp__findReferences` for each export
3. If default export has 0 references outside file -> component is dead

Before removing:
- Check if component is conditionally rendered (search for component name in JSX)
- Check if component is used via dynamic import
- Verify not a layout/page component (those are framework entry points)

For confirmed dead components:
```bash
git rm components/branding/platform-name-preview.tsx
git commit -m "refactor: remove unused platform-name-preview component

- findReferences returned 0 usages
- Associated platform-names page deleted in Phase 14
- Not dynamically imported"
```
  </action>
  <verify>
    - High-priority subdirectories audited
    - Dead components removed with documented commits
    - `pnpm typecheck` passes
    - `pnpm lint` passes
  </verify>
  <done>High-priority component subdirectories audited and cleaned</done>
</task>

<task type="auto">
  <name>Task 3: LSP audit of core component subdirectories</name>
  <files>gestao_fronteira/components/attendance/*.tsx, gestao_fronteira/components/diary/*.tsx, gestao_fronteira/components/grades/*.tsx, gestao_fronteira/components/reports/*.tsx, gestao_fronteira/components/students/*.tsx</files>
  <action>
Audit core component subdirectories (these are actively used but may have legacy exports):

**Attendance components (16 files):**
- Check each .tsx file for unused exports
- Especially look for old components replaced during Phase 15 refactoring

**Diary components (17 files):**
- VivenciasTimeline, VivenciasReference may be legacy
- Check components created before Phase 7 data integrity work

**Grades components (2 files):**
- GradeGrid.tsx, GradeInput.tsx
- Verify both are used in notas page

**Reports components (5 files):**
- Check each report component for usage
- DescriptiveReportForm may have unused exports

**Students components (4 files):**
- Check StudentProfileHeader, StudentTags, StudentInfoGrid, FaixaEtariaIndicator

For each subdirectory:
1. List all .tsx files
2. Check default export and named exports via LSP
3. Cross-validate with knip results from Task 1
4. Remove confirmed dead code

After all removals:
```bash
pnpm typecheck && pnpm lint && pnpm build
```

Note: Be careful with recently refactored components (AttendanceGrid subcomponents, FrequenciaWorkflow subcomponents) - these were just created in Phase 15 and should all be in use.
  </action>
  <verify>
    - All core component subdirectories audited
    - Dead components/exports removed
    - `pnpm typecheck && pnpm lint` passes
    - `pnpm build` succeeds
  </verify>
  <done>Complete components/ directory audit finished, all dead code removed</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pnpm typecheck` - no TypeScript errors
2. `pnpm lint` - no ESLint errors
3. `pnpm build` - build succeeds
4. `npx knip --include exports` on components/ - significantly fewer or zero unused exports
5. Git log shows atomic commits for each component removal
</verification>

<success_criteria>
- All 141 component files audited (excluding ui/)
- Dead components identified and removed
- Zero TypeScript errors after cleanup
- Zero lint errors after cleanup
- Build passes
- Each removal has atomic commit with documented reason
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-dead-code-audit-lsp/15.1-02-SUMMARY.md`
</output>
