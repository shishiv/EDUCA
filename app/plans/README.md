# Implementation Plans — EDUCA monorepo

Generated by the **improve** skill (deep audit) on **2026-07-11**.  
**Planned at commit:** `7e696f8`  
**Repo root:** `~/workspace/dev/EDUCA` · **App cwd for commands:** `app/`

Execute in the order below unless dependencies say otherwise.  
Each executor: read the plan fully, honor STOP conditions, update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 007 | RLS write policies for multi-tenant tables | P0 | L | — | TODO |
| 009 | Fix `users.role` → `tipo_usuario` on mark API | P0 | S | — | TODO |
| 008 | Auth + role checks on attendance server actions | P0 | M | 007 ideal; 009 parallel OK | TODO |
| 011 | Unifyify `tipo_usuario` / roles app-wide | P1 | M | — | TODO |
| 010 | Align middleware route map with real routes | P1 | M | 011 | TODO |
| 012 | CI runs unit tests; lint gates | P1 | S | — | TODO |
| 001 | Fix critical dependency vulnerabilities | P1 | S | — | TODO |
| 004 | Remove unused `posthog-js` | P2 | S | — | TODO |
| 002 | Consolidate Supabase server client usage | P1 | M | — | TODO |
| 003 | Eliminate `as any` in API layer | P2 | M | 002 | TODO |
| 013 | Harden `/api/health` exposure | P2 | S | — | TODO |
| 006 | Clean up Zustand stub store | P3 | S | — | TODO |
| 005 | In-memory API rate limiting | — | — | — | **REJECTED** |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED (reason)` | `REJECTED (reason)`

## Recommended waves

```
Wave A (P0 security/correctness, parallel):
  007 RLS  |  009 role column  |  012 CI tests  |  001 vulns

Wave B:
  008 server action auth  (after 007 if possible)
  011 unify roles
  004 remove posthog

Wave C:
  010 middleware map  (after 011)
  002 supabase client consolidate
  013 health harden

Wave D:
  003 as any cleanup
  006 stub cleanup
```

## Dependency notes

- **008** is weaker without **007** (auth in app without DB write policies is incomplete defense).
- **010** should follow **011** so middleware keys match `tipo_usuario`.
- **003** after **002** so typing targets the canonical server client.
- **005** rejected: in-memory rate limits on Vercel serverless are not real protection. Reopen only with Upstash/WAF.

## Findings considered and rejected / deferred

| Item | Verdict |
|------|---------|
| In-memory rate limit (old 005) | REJECTED — isolate-local Map |
| Provider-agnostic adapter | Deferred — `docs/PROVIDER-AGNOSTIC-ROADMAP.md` |
| Marketing in this monorepo | N/A — ADR-002 |
| Split `types/database.ts` | Rejected — generated |
| Dual `aulas_abertas` / `sessoes_aula` cutover | Deferred (L) — not planned this batch; do after P0 |
| Vivencias 501 stubs | Keep — intentional pre-feature |

## Plan files

```
app/plans/
  README.md
  001-fix-critical-vulnerabilities.md
  002-consolidate-supabase-client.md
  003-eliminate-as-any-type-escapes.md
  004-remove-posthog.md
  005-add-rate-limiting.md          # REJECTED
  006-clean-up-stubs.md
  007-rls-write-policies.md         # P0
  008-server-action-auth.md         # P0
  009-fix-role-column-bug.md         # P0
  010-middleware-route-map.md
  011-unify-roles.md
  012-ci-run-tests.md
  013-health-endpoint-hardening.md
```

## Verification baseline (every plan)

From `app/`:

```bash
pnpm typecheck
pnpm test
pnpm lint          # should become gating after 012
```

DB plans (007): also apply migration + smoke professor write / cross-tenant deny.

## Advisor note

These plans are for **executor agents**. The improve advisor does not implement source changes. Use `improve execute <plan>` if the host supports isolated worktrees, or run plans manually in order.
